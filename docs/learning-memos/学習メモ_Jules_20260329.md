# 学習メモ (2026-03-29)

## タスク概要
- 夜間カオステスト・異常系堅牢化ミッションとして、`tests/test_nightly_robustness.py` にエッジケースおよび境界値テストを追加。
- 既存のプロダクションコード（.py）は一切変更せず、独立したテストケースのみで機能性を検証。

## 学んだこと・注意点
- `Task` Pydantic モデルの `tags` フィールドはリストではなくカンマ区切りの `str` 型を想定している。
- `database.py` はファサード層であり、タスク作成は `database.create_task(Task(...))` を呼び出す構成になっている。
- 100% PASS を保証するため、既存のテストスイーツ全体を `xvfb-run python3 -m pytest -q` で回して正常動作を確認した。
