# 学習メモ (Jules_20260925)

**作成日時**: 2026-09-25
**担当エージェント**: Jules
**対象リポジトリ**: ネオ秘書くん (Neo-Secretary)

---

## 1. 実施タスクおよび対応内容

### 優先順①: 【P1】ID 36 LifeCoachEngine の撤去 ＆ ID 38 ドキュメント同期
- **背景と目的**:
  - 不要な機能の引き算ポリシーに基づき、2時間ごとのLLM呼び出しコストに対し行動変容効果が薄かった `LifeCoachEngine`（生活コーチ機能）を完全撤去。
  - `LifeDreamer`（天気・生活イベントによるペット情緒生成）および PWA の TTS 読み上げ機能 (`toggleBriefingSpeech`) はコアバリューとして温存。
- **対応内容**:
  - `life_coach_engine.py` および `tests/test_life_coach_engine.py` を削除。
  - `main.py` より `get_life_coach_engine().start()` 起動処理を削除。
  - `local_sync_server.py` の `/api/status` ペイロードから `life_coach_engine` インポートを削除し、`"life_coach": None`（互換性維持）へ変更。
  - `tests/test_scheduler_periodic.py` から `LifeCoachEngineSchedulerMigrationTest` を削除。
  - `neo_hisho.spec` から `'life_coach_engine'` を削除。
  - `i18n.py` から `"coach.*"` 翻訳辞書キーを削除。
  - `docs/specs/DESIGN_SPEC.md` §26 および `docs/specs/機能ロードマップ.md` §13.19 (ID 36 / ID 38) のステータスを撤去完了 (`✅ 完了 2026-09-25`) に更新。
  - TDD 検証テスト `tests/test_life_coach_removal.py` を作成し、`LifeCoachEngine` の非存在と `LifeDreamer`/TTS の残存を回帰検証。

---

### 優先順②: 【P3】ID 62 web_pet バンプ規約テスト新設 ＆ 音声関連表記の統一
- **背景と目的**:
  - `web_pet` 配下の JS/HTML/CSS 変更時に `version.py` (`__version__`) のバージョンバンプを自動検証する規約テストを新設。
  - `character_manager.py` における旧表記「内蔵音声認識は」を統一表記「内蔵の音声文字起こし機能」へ統一。
- **対応内容**:
  - `tests/test_web_pet_version_bump.py` を作成:
    - `version.py` (`__version__`) が `web_pet/version.js` (`APP_VERSION`, `WEB_PET_CACHE_NAME`) および `web_pet/index.html` (`?v=`) と完全一致することを検証。
    - `web_pet/` 配下ファイル更新時に `version.py` が同時に更新・バンプされることを git diff 経由で機械検証。
    - `character_manager.py` 内の表記が「内蔵の音声文字起こし機能」に統一されていることを検証。
  - `character_manager.py` 内の提示テキスト「内蔵音声認識は」を「内蔵の音声文字起こし機能は」に置換・統一。

---

## 2. 開発規約遵守チェック

- [x] **AGENTS.md / OPENCODE.md 規約**:
  - Python 型ヒント、日本語 Docstring、`logging` モジュール適用、例外握りつぶし防止を徹底。
- [x] **TDD (Red ➔ Green)**:
  - 事前に失敗するテストケース (`tests/test_life_coach_removal.py`, `tests/test_web_pet_version_bump.py`) を作成し、Red 状態を確認した後に実装・修正を行って Green 移行を確認。
- [x] **全回帰テスト**:
  - `xvfb-run pytest -q` 実行結果: **856 passed, 62 skipped, ALL GREEN**。

---

## 3. 今後の申し送り・注意事項

- **LifeDreamer ＆ TTS の健全性**:
  - `life_dreamer.py` (天気・イベント感度) および `web_pet/pet.js` の `toggleBriefingSpeech` (朝会読み上げ) は正常稼働を確認済みです。
- **バージョン同期**:
  - `web_pet` 配下を変更する際は、規約テスト (`test_web_pet_version_bump.py`) により `version.py` の `__version__` バンプが自動チェックされます。
