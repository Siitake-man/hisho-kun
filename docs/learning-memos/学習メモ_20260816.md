# 📚 学習メモ_20260816
**最終更新日時**: 2026-08-16 20:15

---

## 📌 本日の学習サマリ（Executive Summary）
1. **Deep Module 設計による God Object（肥大化ファイル）の解体**:
   2,120行に肥大化していた `gui.py` を、独立した責務を持つサブモジュール群（`ui/qr_dialog.py`, `ui/settings_window.py`, `ui/calendar_window.py`, `ui/sticky_note.py`, `ui/db_viewer.py`）へ分割し、`gui.py` 自体を軽量なオーケストレーター（約500行）に深化させた。
2. **Web Audio API による倍音合成チャイムの構築**:
   スマホスピーカーでも確実に認知できるよう、高音域（`E5`〜`E6`）のサイン波にトライアングル波の倍音を重ね合わせた「ピンポンパンポーン♪」2連打チャイムとバイブレーションパルスを実装。
3. **外部生成AIを活用したキャラクタースプライト設計方針の確立**:
   AIエージェントによるリアルタイム生成の遅延とテイストのブレを解消するため、Midjourney/DALL-E 3用の高精度プロンプト仕様書（`docs/CHARACTER_PROMPTS_SPEC.md`）を策定し、ユーザー自作による高品質スプライト組み込みパイプラインを確立した。
4. **Pillow純粋実装スライサーと 1-bit Alpha（完全2値化透過）**:
   Windows Tkinter（`-transparentcolor #FF00FF`）で半透明ピクセルがピンク色のゴミとして画面に残る問題を、Alphaチャンネルの完全2値化（0 or 255）で完全解決。また `numpy` 依存を排除し、仮想環境標準の Pillow のみで堅牢に動作させた。
5. **他者配布・ポータブルMCP自動セットアップ (`mcp_installer.py`)**:
   ハードコードされた絶対パスを排し、`sys.executable` と `Path(__file__)` から実行環境を自己検出して、Antigravity / Claude Desktop の設定JSONへワンクリックで自動登録する仕組みを実装。

---

## 🧠 PM・アーキテクト視点での概念解説（What / Why / Where）

### 1. Deep Module（深いモジュール）と Seam（接合面）
- **What**: 外部に対しては「シンプルで扱いやすい小さなインターフェース」を公開し、内部に「複雑な処理や状態管理」を深く隠蔽する設計原則。
- **Why**: 1つのファイルに全UI・全イベントハンドラ・全ダイアログを詰め込むと、わずかな修正で思わぬ副作用（バグ）が生じ、AIや人間の認知的負荷が跳ね上がるため。
- **Where/When**: 設定画面、カレンダー手帳、付箋、DBビューアなどの独立した業務画面を開発・保守する際に適用。

### 2. Web Audio API によるシンセシス（音声合成）
- **What**: 音声ファイル（MP3/WAV）をダウンロードして再生するのではなく、ブラウザ内部のオシレーター（発振器）でリアルタイムに音波を合成して鳴らす仕組み。
- **Why**: ネットワーク遅延ゼロ、外部音声ファイル読み込みエラーゼロで、8-bitレトロゲームのような心地よいチャイム音やタップ音を軽量に再生できる。
- **Where/When**: スマホDesk Pet端末における承認要請アラート、作業完了通知、ボタンクリック音。

### 3. Self-Resolving Installer（自己解決型インストーラー）
- **What**: 外部設定ファイルに書き込むパスを固定文字列ではなく、実行中のプロセス自身のパス情報から動的に生成・注入する設計。
- **Why**: 開発環境の特定ユーザー名（`C:\Users\bonob`）に依存せず、他人のPCや別ドライブに配布しても100%正常に連携させるため。
- **Where/When**: MCPサーバーの Antigravity / Claude Desktop / Cursor へのワンクリック自動登録。

---

## 💡 身近な例えでの理解

- **Deep Module化**:
  > 「何でも屋の巨大な総合受付（1人の担当者が電話・経理・人事・警備を全部こなす）」から、**「各専門部署（人事課、経理課、受付窓口）に部屋を分け、総合受付は案内役だけに専念する」** 組織改編と同じ。
- **Web Audio 倍音合成**:
  > 1本のフルート（基音）だけでは音が細くて騒がしい部屋では聞こえにくいが、**「バイオリン（倍音）を一緒に重ねて演奏する」** ことで、音が分厚くリッチになり、遠くまで透き通って届くようになる仕組み。
- **MCP自己解決セットアップ**:
  > プリンターを買ったときに、ユーザーが手で設定ファイルを書き換えるのではなく、**「簡単セットアップCDが勝手にPC内のWordやExcelを見つけて印刷ボタンを追加してくれる」** のと同じ。

---

## 🛠️ 本日習得・適用した技術スタック

1. **Python / CustomTkinter**:
   - `CTkToplevel` のモジュール分離と循環参照を防ぐパッケージ構成（`ui/__init__.py`）。
   - クラス継承と Tkinter イベントループ（`after` / `bind`）の安定化。
   - `webbrowser.open` による吹き出しからのURLワンクリックブラウザ起動。
2. **JavaScript / Web Audio API**:
   - `OscillatorNode`（サイン波 ＋ トライアングル波）と `GainNode` による指数減衰エンベロープ（`exponentialRampToValueAtTime`）。
   - モバイルブラウザにおける User Gesture アンロックと `AudioContext.resume()`。
3. **Pillow 画像処理 ＆ 1-bit Alpha 透過**:
   - `Image.getdata()` / `putdata()` による純粋Pythonでの高速ピクセル走査と、白背景（RGB>240）の完全透明化・キャラ部の完全不透明2値化。
4. **SQLite Online Backup API ＆ WALモードによる耐障害性設計**:
   - `conn.backup()` による稼働中ホットバックアップ（最新7世代ローテーション）と、`PRAGMA journal_mode=WAL` によるGUI/HTTPサーバー間の並行ロック解消。
5. **FastMCP / JSON-RPC 2.0 ツール拡充 ＆ 自動登録**:
   - `create_task`, `complete_task`, `create_calendar_event` を追加し、外部AI（Codex/Claude Code等）から秘書くんの手帳・カレンダー・MentisDBを直接操作できるAPIサーフェスを確立。
   - `mcp_installer.py` による各AIツール設定ファイルへの動的マージ。

---

## 📈 現在の進捗と次のステップ (PM Roadmap)

- **Phase A**: ✅ 完了（AIニュースRSS、カレンダー週間/24hタイムグラフ、Web Audio、常時ON）
- **Phase B**: ✅ 完了（`gui.py` のモジュール分割・Deep Module化）
- **Phase C**: ✅ 完了（プロンプト仕様書 `docs/CHARACTER_PROMPTS_SPEC.md` 策定）
- **Phase D**: ✅ 完了（通知音の2和音高音チャイム化・バイブレーション強化）
- **Phase E**: ✅ 完了（外部MCPサーバー連携の堅牢化 ＆ ローカルDB自動バックアップ）
- **Phase F〜H (次期ロードマップ)**: 🎯 ゲーム級UX（ポモドーロゲージ・エフェクト合成・カード型バッジ・MCP自動セットアップ）

🎉 **本日の全予定タスクおよび次期マスター作業計画書の策定が完了しました！**
