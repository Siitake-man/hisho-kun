# ネオ秘書くん 機能ロードマップ (機能ロードマップ.md)

- **バージョン**: 1.5.2 (⚡ 開発スタイル全面刷新 ＆ 認知負荷の引き算 ＆ Jev 29kトークン二分探索スキル棚卸し ＆ 記憶の引き算完了)
- **最終更新日時**: 2026-09-23 23:05 (⚡ MentisDB完全撤去／Jev 29kトークン二分探索棚卸し・38件安全退避／Jev Harness 4大進化／P0×4 全封鎖 ＆ ID 50 端末UUID恒久化 ＆ ID 59 Colab/Kaggle初期セットアップ起票)
- **目的**: TickTick対比70+機能、Agent Bridge、知識の宝庫、省電力アーキテクチャ、およびJev意思決定エンジン/エコシステム全体の開発現在地と進捗ステータスを同期管理する。

---

## 0. 実装進捗サマリ（現在地）
- 🟢 **⚡ 開発スタイル全面刷新 ＆ 認知負荷の引き算（Jev 29k二分探索・38スキル退避・MentisDB完全消滅） (2026-09-23 完了 / v1.5.2)**:
  - **MentisDB 完全消滅（記憶装置の引き算）**: `mcp_config.json` および Antigravity IDE設定から MentisDB を完全除去し、`cargo uninstall` でバイナリも完全消去。用語境界テスト（`tests/test_terminology_boundary.py`）により、エージェント用コードへの混同再流入を機械的に封鎖（ALL GREEN）。記憶基盤は秘書くん本体の「知識の宝庫」へ一本化。
  - **Jev 29,160トークン強制収束 ＆ 全115スキル棚卸し監査**: 日本語・記号のトークン密度急増による `max_tokens_exceeded (HTTP 400)` を二分探索（バイナリサーチ）アルゴリズムで突破し、目標レンジ（`27,000 <= tokens <= 29,500`）へ決定論的に収束（実測 **29,160 tokens** で完走）。誤分類をプレフィックス完全一致で是正し、企業用GCP24件＋UI重複14件（計38件）を `~/.gemini/config/skills_archive/` へ安全移動完了。
  - **Jev Harness 4大進化**: `plan_and_guard()` および MCPツール `jev_plan_and_guard` を新設。危険コマンドの自己審査とDesk Petワンタップ承認を直結。
  - **知識の棚卸しライフサイクル ＆ ラップアップ二重資産化**: 蒸留蔵思想のエビデンス検証ゲートを `storage/insight_repo.py` に実装。知識の宝庫へ「知見ID: 14（29kトークン二分探索スキル棚卸し哲学）」を確定永続化。ELI5動く図解HTML配備。
- 🟢 **全権限インターセプト通知配備 ＆ 📦 全エージェント統合 AI Dotfiles バックアップ基盤 (2026-09-22 完了 / v1.5.1)**:
  - **全権限インターセプト通知**: コマンド実行だけでなく、ワークスペース外（`~/.config/` 等）への `list_dir`, `view_file`, `write_to_file`, `replace_file_content` 実行時に IDE が出す権限確認ダイアログをリアルタイム検知。即座に PC ペットとスマホ Desk Pet へ通知を発火させ、作業停止を未然に防止。
  - **全エージェント統合 AI Dotfiles Git管理**: 4大エージェント（Antigravity, OpenCode, Cline, Codex）の設定・スキル・ルールと秘書くん「知識の宝庫 (`user_insights`)」を単一Gitリポジトリ（`~/.ai_dotfiles`）で差分管理する `tools/sync_ai_dotfiles.py` を新設。日々のZIP肥大化を解消し、新端末へ1コマンドで復元可能。
  - **セッションラップアップ連携**: `session-wrap-up` 実行時のGitコマンド提示において、AI Dotfiles の自動バックアップ＆プッシュコマンドを不可分の1セットとして提示する手順を正式制定。
- 🟢 **設定画面モダン左サイドバー Navigation Rail 刷新 ＆ 🤖 エージェント Hooks 設定ガイド配備 (2026-09-22 完了 / v1.5.0)**:
  - **タブ文字潰れの根本解決**: 従来の幅500px横並びタブを廃止し、幅760x600（minsize 720x550）の左右2カラム構造（左サイドバー190px Navigation Rail + 右コンテンツコンテナ）へ全面移行。日本語・英語問わず文字切れが物理的に生じないレイアウトを確立。
  - **エージェント Hooks 設定ガイド配備**: Nav 2 に「🤖 エージェント連携」を新設。Antigravity / OpenCode / Claude Code 用の設定スニペットのワンクリックコピー、およびワンタップで実機テストできる「🔔 接続テスト（Ping）」ボタンを配備（バックグラウンドスレッド実行 ＆ 連打ガード ＆ 前面ダイアログ固定）。
  - **リアルタイム多言語追従**: 一般タブで日本語 ⇆ English を切り替えた際、アプリ再起動なしでサイドバーボタン全6項目、ヘッダー、主要カードのタイトル群が即時更新されるイベント駆動Seamを確立。
  - **二重定義（Ghost Frame）解消 ＆ カオス耐性**: サブエージェント（`devils-advocate`, `agent-tester`, `quality-reviewer`）の協調監査により、`content_tools` の二重定義解消、同一タブ連打フリッカーガード、破棄後TclError防止、テストの決定論的同期を完備。全件 APPROVED。
- 🔲 **新・機能オンボーディングツアー日英完全対応 (次期スプリント計画)**:
- 🟢 **OpenCode実証済み「ask_input非ブロッキング通知」のAntigravity移植完了 (2026-09-21 完了 / v1.4.4)**:

  - **二大エージェント共通化**: OpenCode 側で実証された `POST /api/agent/ask_input`（`wait_decision: false`, `timeout: 5`, `choices: []`）を Antigravity の `PreToolUse` Hook（`tool_guard_hook.py`）へ完全移植。
  - **PCペット ＆ スマホ覚醒**: 承認待ち発生時に PCペットが `alarm_ask`（6秒）で大暴れし、スマホDesk Petが激しいBuzz振動と画面演出を発火。
  - **スパム抑止 ＆ Fail-Safe**: コマンド別SHA256ハッシュによる60秒クールダウンを配備。`agent_bridge_client._post_to_hub` の再利用によりポート・トークン・例外隔離を完全担保。
  - **TDD品質ゲート**: サブエージェント `agent-tester` により `tests/test_tool_guard_hook.py` 全9テストケース ALL GREEN を確認。
- 🟢 **スマホ承認ダイアログ多重ポップアップの完全根治 ＆ Antigravity Tool Guard Hook配備 (2026-09-21 完了 / v1.4.3)**:

  - **二重デバウンス防壁**: サーバー側 `ui/device_approval_dialog.py` に `_recent_approvals`（10秒デバウンスキャッシュ）を敷設し、同一端末（`client_ip:device_name`）からの短時間再要求を即時 `True` でバイパス。クライアント側 `web_pet/pet_auth.js` に 3秒間のインターバルクールダウン（`TOKEN_REFRESH_COOLDOWN_MS = 3000`）を設け、ダイアログの連続多重ポップアップを完全根治。
  - **Antigravity Tool Guard Hook**: 公式の `PreToolUse` Lifecycle Hook (`.agents/hooks.json`, `~/.gemini/config/hooks.json`, `tool_guard_hook.py`) を配備。関数名・クラス名などの識別子検索や危険コマンドを物理的に遮断（deny）して `codebase-memory-mcp` へ決定論的に誘導。
  - **Jev MCP 堅牢化**: API通信の一時エラー時に空辞書をサイレント返却していた問題を是正し、最大2回試行の自動リトライと透過的エラー通知を実装。
  - **品質ゲート**: サブエージェント `quality-reviewer` の多角査読により APPROVED を獲得。
- 🟢 **OpenCode モデル選択 ＆ 入力待ち通知の完成 (2026-09-21 深夜 完了 / ボス承認済み)**:
  - ⚡ **Jev モデル選択（3ツール版MCP）**: Antigravityの直列化制約を回避するため**契約の境界を分離**（Antigravity=2ツールのまま／OpenCode=3ツール）。`jev_select_models` は**候補をクライアントが渡す**設計で、GO契約の実在モデルを `tools.opencode.models` から**ライブ取得**（キャッシュ陳腐化を根絶）。
  - 💰 **コスト帯ポリシー（`model_policy.json`）**: 出力$1.0/M未満=常用帯（自動）／以上=承認帯（**スマホ承認必須**）。実装の既定は `deepseek-v4.1-flash` / `glm-5.3-flash`（ボス指定）、監査は `glm-5.3` / `grok-4.6` / `kimi-k3`。**B案（厳格運用）**＋**ドリフト検知CLI**（廃止・新規・価格改定を機械検知）で、ボスが1行追記するだけで追随可能。
  - 🐬 **入力待ち通知プラグイン（`hisho-approval-notify`）**: `ctx.permission.hook("evaluate")` の `effect:"ask"` のみを通知（スパム防止）→ PCペット `alarm_ask` ＋ スマホプッシュ。**ボスが実機で受信確認済み**。Antigravity側の `PreToolUse` Hook と対をなす。
  - 🧪 **品質**: 独立査読（quality-reviewer）で P0×1 / P1×6 を検出し全件是正（TDD Red→Green 実証）。全回帰 **753 passed / 2 skipped**。
- 🟢 **OpenCode × Jev-MCP 自律マルチエージェント体制 実機配備完了 (2026-09-21 完了 / OpenCode v2.0.11)**:
  - `docs/guides/OPENCODE_JEV_MULTIAGENT_INTEGRATION_SPEC.md` を v1.1.0 へ改訂し、V1/V2 形式差異を是正 + §0 As-Built を新設。
  - **MCP配線**: `~/.config/opencode/opencode.jsonc`（`jev-mcp` / `codebase-memory-mcp`）＋ `<repo>/opencode.json`（`neo_hisho_bridge` + Hard ACL 31ルール）。
  - **11体配備**: `.agents/agents/*.md` を正本とし、`tools/sync_opencode_agents.py` で OpenCode V2 `permissions` へ自動変換（`--check` ドリフト検知付き）。
  - **二層防御**: Jev Guard（確率的ソフト層）＋ `permissions` 15 deny（決定論的ハード層）。
  - **実機検証**: `agent-tester` 実起動 → 回帰テスト **684〜686件 ALL GREEN**（failed=0 / errors=0）。Jev Route は「UIボタンのタッチ判定を修正する」で `pixel-frontend-designer` 66.0% を推薦。
  - **規約**: `<repo>/OPENCODE.md` 新設 ＋ `AGENTS.md` §4「OpenCode 実行環境プロトコル」＋ `~/.config/opencode/AGENTS.md`（GEMINI.md への薄いブリッジ）。
- 🟢 **Jev System One × OpenCode 多種多様なサブエージェント連携仕様策定 ＆ 二重資産化 (2026-09-21 完了 / v1.1.6)**:
  - OpenCode Desktopへの完全導入仕様書（`docs/guides/OPENCODE_JEV_MULTIAGENT_INTEGRATION_SPEC.md`）を策定。AntiGravity（`.agents/agents/`）のスペシャリスト資産（実装時点で11体）との完全同期、`opencode.json` 設定、およびキックオフ指示プロンプトを完備。
  - 本日の核心を解剖する動くSVGシミュレータ図解（`docs/explainers/eli5_20260921_jev_system_one_and_opencode_multiagent.html`）を生成。
- 🟢 **スマホUI言語切替ボタンのタッチ確実化 ＆ 視覚フィードバック ＆ PWAキャッシュパージ (2026-09-21 完了 / v1.1.6)**:
  - スマホUI側の上部バー言語切り替えボタン（`#lang-toggle-btn`）のタッチターゲット拡大（幅36px×高さ26px）、`touch-action: manipulation`（ダブルタップズーム遅延根絶）、`z-index: 20`、および `click` と `touchend` のダブルバインド（300msガード付き）を配備。
  - 切り替え完了時に `showToast()` を発火させ、スマホ画面中央に「🌐 Switched to English」/「🌐 言語を日本語に切り替えました」の金色の切替トーストを即時表示（視覚・触覚フィードバックの確立）。
  - キャッシュバスターを `v1.1.6` にインクリメントし、起動時に旧キャッシュ（`neo-pet-v1.1.5` 以前）を自動削除するクリーンアップを発火。
- 🟢 **スマホ⇆PC双方向言語同期 ＆ 手動切替巻き戻しガード (2026-09-21 完了 / v1.1.5)**:
  - スマホ側ヘッダー言語トグルボタン（[EN]/[JA]）および設定モーダルの言語チップ切替時、PCサーバー（`/api/action` `set_language`）へ双方向リアルタイム同期。
  - スマホ側で言語を切り替えた直後5秒間は、サーバーからの古いステータスによる上書き巻き戻しを無視する `isUserOverrideActive()` ガードを敷設し、日本語への即時巻き戻しを完全根絶。
- 🟢 **HTTPスレッド安全性完全根絶・多重要求合流 ＆ 端末台帳削除機能 (2026-09-21 完了 / v1.1.4)**:
  - `RuntimeError: main thread is not in main loop` を完全解消。HTTPワーカースレッドからの Tkinter メソッド（`winfo_exists` 等）呼び出しを撤廃し、ダイアログ生成を `gui.post_action` でメインGUIスレッドへ完全移管。
  - 同一端末からのポーリング重複要求を既存の承認ダイアログ結果イベントへ合流待機（Coalesce）させ、ビジー拒絶自滅ループを根絶。
  - スマホ側 `pet_auth.js` に `requestSyncToken()` による Promise 重複排除を配備。
  - 端末台帳（`storage/device_repo.py`, `database.py`）に同一端末の更新再利用ロジックおよび `delete_device(device_id)` を実装。設定画面（`ui/device_manager_panel.py`）に確認ダイアログ付き「🗑️ 削除」ボタンを新設。
  - HTTPワーカースレッドから `actual_root.after` を呼んでいたことによる Windows スレッドメッセージキューでのドロップ（ダイアログが表示されずに30秒タイムアウトする不具合）を特定。
  - `gui.post_action`（`_action_queue` 経由）によるメインスレッドへの確実なディスパッチに変更。
  - `dialog.wait_window()` を廃止し、`on_decision` コールバックによる完全ノンブロッキング設計へ刷新。
- 🟢 **P0端末承認ダイアログエラー根絶 ＆ デスクトップペット吹き出し多言語化 (2026-09-20 完了 / v1.1.2)**:
  - スマホ初回接続時に `NeoSecretaryGUI object has no attribute winfo_exists` で端末承認ダイアログ呼び出しが即座に例外落ちし、Fail-Closed によりスマホが接続拒否（403）されていた致命的バグを完全解消。
  - `actual_root = getattr(root, "root", root)` により `NeoSecretaryGUI` と `tk.Tk` の双方を安全に受け入れる二重防御を配備。
  - デスクトップペット吹き出し（スマホ自動最小化トグル、徘徊モード、LLM切替、スマホ呼び出し等）を `i18n.t()` 化。
- 🟢 **デスクトップ ⇆ スマホ双方向多言語同期 ＆ PWA全域多言語化 (2026-09-20 完了 / v1.1.1)**:
  - デスクトップ側言語変更（`/api/status`）とスマホPWA（`NeoLang.setLang()`）の双方向動的同期を配備。
  - PWAフロントエンド（`web_pet/lang.js`, `pet_ui.js`, `pet.js`）において、メイン画面バナー・サジェスト・ドックおよび動的モーダル（TODO、カレンダー、習慣、ノート、設定）の `NeoLang.t()` 化を完全実装。
  - 設定モーダル内に `🇯🇵 日本語` / `🇺🇸 English` 切替チップを追加し、`neolang:changed` イベント駆動型ですべてのDOM・モーダルを瞬時に再描画する機構を確立。
  - キャッシュバスターを `v1.1.1` にインクリメントし、PWA Service Worker キャッシュ対象に `lang.js` を追加。
- 🟢 **P0/P1時限爆弾撤去 ＆ 多言語化Phase 4完遂 (2026-09-20 完了)**:
  - 5大専任ペルソナ深層監査レポート（`docs/code_review_report_20260920_deep_audit.md`）で指摘されたP0/P1潜在バグを迅速是正。
  - `main.py`: HTTPワーカースレッドからの `asyncio.create_task` 即死バグを `asyncio.run_coroutine_threadsafe(..., app.loop)` に是正。
  - `main.py` / `proactive_engine.py`: カレンダー予定リマインダーの二重発火競合を解消し、`reminder_engine.py` に完全一本化。
  - `storage/task_repo.py`: 繰り返しタスク自動生成時の `list_id` / `tags` カラム逆転データ汚染バグを修正。
  - `local_sync_server.py`: `ServerWatchdog` の失敗許容回数を 1 ➔ 3、タイムアウトを 1.0s ➔ 2.0s に緩和（ポート10048競合・自爆ループ防止）。
  - `agent.py`: システムプロンプト内の日本語規則ハードコードを動的言語指示 `get_prompt_language_instruction()` に置換。
  - `i18n.py`: 起動時に `.env` の `APP_LANGUAGE` を自動ロードする仕組みを配備。
  - `ui/sticky_note.py`: 半透明スマート付箋のタイトル、プレースホルダー、空タスク通知、緊急度バッジを `i18n.t()` 化し、動的言語切替リスナーに対応。
  - `tests/test_i18n_and_ui_protection.py`: 付箋の多言語キーおよび環境変数言語リロードの単体テストを追加。
- 🟢 **Sprint Global: アプリ多言語化 ＆ UIレイアウト耐性6大防壁 完遂 (2026-09-20 完了)**:
  - 英語化に伴うテキスト長膨張（1.3〜1.8倍）に対する「引き算のMicrocopy規約（文字数予算）」を完全実装。
  - PWA側辞書（`web_pet/lang.js`）、トグルボタン（`[EN] / [JA]`）、ボトムドック短縮表記（`Tasks / Cal / Daily / Config`）、承認ボタン sticky 固定化を完了。
  - デスクトップ側 `i18n.py` にUI辞書、**LLM動的言語ガード**（選択言語に応じた中国語・英語・日本語の厳格排除）を実装。
  - 悪魔の代弁者（`devils-advocate`）のカオス破壊監査に基づき、1000文字爆弾によるペット窒息死防止（clamp 140px / Truncate 180文字）、長大トークン強制分割、型安全・ValueError即死防御の**6大防壁**を全網羅。
  - `tests/test_i18n_and_ui_protection.py` による全テスト完全合格。
- 🚀 **v1.1.0 正式リリース策定 (2026-09-20 完了)**:
  - ゼロトラスト端末認証、Jev安全審査バッジ、適応型Canvas 0fps省電力、CI/CD完全自動化（全7ジョブALL GREEN）を統合した大型マイナーリリース。
- 🟢 **Jev 安全審査バッジのデータ伝達断絶根治 ＆ PWA v1.1.0 適用 (2026-09-20 完了)**:
  - `local_sync_server.py` の `AgentBridgeRequest` に `summary` / `safety_level` を追加し、`to_dict()` で出力。
  - `api_agent_bridge.py` の `handle_agent_ask` で `summary` から Jev 審査レベルを自動抽出。
  - `web_pet/pet_ui.js` の `buildJevBadge()` で `summary`, `title`, `content` 全体を包括スキャンし、ボトムシートのタイトルにもフォールバック適用。
  - `version.py`, `web_pet/version.js`, `web_pet/index.html` を `1.0.4` にインクリメント。
  - `tests/test_agent_bridge_jev_badge.py` による TDD 回帰防止テストを追加。
- 🟢 **Sprint C: GitHub Actions CI/CD パイプライン全7ジョブ ALL GREEN 達成 (2026-09-20 完了)**:
  - `.github/workflows/ci.yml` 完備。Ubuntu (3.11, 3.12, 3.13) ＋ Windows (3.12, 3.13) の全5テストジョブ、Lint、Secret Scanの全7ジョブ完全合格。
  - Ubuntu ヘッドレス環境での `xvfb` 仮想フレームバッファ導入（Tkinter/CustomTkinter GUIテストの完全実走）。
  - GitHub CLI (`gh`) との連携により、AIがブラウザを開かず自律的にCIログを取得・解析・自己修復するループを実証・確立。
- 🟢 **P0-2 ＆ P0-3 完全制覇・全11テスト完全合格 (2026-09-20 完了)**:
  - 未承認外部端末接続時の Human-in-the-Loop 承認ダイアログ (`ui/device_approval_dialog.py`)。
  - `_is_dialog_active` 排他ミューテックス（ダイアログスタックDoS防止）＆ `cb is None` 403 Fail-Closed 遮断。
  - Desk Pet PWA 承認カードへの Jev リアルタイム安全審査バッジ（`web_pet/pet_ui.js`）組み込み ＆ 二重絵文字解消。
  - `tests/test_device_connection_approval.py`: 全11テスト 0.410秒 PASS。
- 🟢 **Jev意思決定エンジン（System One）のアーキテクチャ的大進化 (2026-09-20 完了)**:
  - 引き算の美学: 外部LLM直列実行を撤去し、15ms超高速構造化判定に純化。
  - 全119スキル100%ノーカット全文渡し（最長1,056文字対応、キーワード欠落ゼロ）。
  - 2段階選抜（リランキング）方式: 確率スコア付きランキング（スキルTOP 7、MCP TOP 5、エージェントTOP 5）出力。
- 🟢 **グローバルJev動的ルーター ＆ ボス開発エコシステム設計書 (2026-09-20 完了)**:
  - Jev動的ルーターを全プロジェクト共通資産として `~/.gemini/tools/jev_router/` へ完全独立化。
  - 最新モデル動的取得 ＆ ローカルキャッシュ (24h TTL) モジュール `model_fetcher.py` 実装。
  - ボス開発エコシステム・マスター設計書 (`~/.gemini/docs/BOSS_DEVELOPMENT_ECOSYSTEM.md`) 策定。
  - 動的相関図・Showcase (`docs/ecosystem_showcase.html` / `~/.gemini/docs/ecosystem_showcase.html`) 完成。
  - 秘書くんリポジトリ内の一時ファイルを退役させ、関心事の分離（引き算の美学）を達成。
- 🟢 **ゼロトラスト認証・端末台帳・スマホ復帰API (2026-09-18 完了)**:
  - 端末固有暗号論的トークン発行・ハッシュ永続化・un-revoke API・設定画面UI復帰ボタン完遂。  
- **コアミッション**: 「**席を外してもAIコーディングエージェントが止まらない**」。古いスマホを卓上承認リモコン＆相棒ペットに転生させ、開発者の生命時間を最大化する。

---

## 🏛️ プロダクト三層構造の基本方針（2026-09-13 確定）

第三者AIブラインドレビュー（ChatGPT / Grok / Genspark）およびGrilling壁打ちを経て、ロードマップの全項目を以下の3階層へ再定義し、**「何を作り、何を削ぎ落とすか」** を厳格に規律化。

| レイヤー | 役割 | 対象機能 | 開発方針 |
|:---|:---|:---|:---|
| **コア (A)** | **主役・独自の痛点解決** | **AIエージェント遠隔承認** (Claude Code / Cline / Cursor 等)、ゼロトラスト通信、Approval Adapter | 🚀 **最優先投資**。90日後に使われていたい本命。失敗モード堅牢化・実機QA自動化 |
| **世界観 (B)** | **愛着装置・ブランド** | ドット絵ペット、呼吸アニメ、自作キャラMod基盤、秘密の部屋、イースターエッグ | 🔨 **Mod基盤のみ最小実装**。キャラ量産はコミュニティ開放。既存ミニゲーム・演出は現状凍結 |
| **居場所 (C)** | **常駐の理由・背景** | 手帳、TODO、習慣ヒートマップ、iCal読み込み、ポモドーロ、天気 | 🧊 **現状維持**。承認リモコンを常駐させる居場所として完成済み。TickTick等と戦わない |
| **保留 (Backlog)** | **開発リソース浪費の回避** | 音声対話 (Whisper/TTS)、Google/LINE/Notion双方向同期、Life Coach L2/L3、新規ミニゲーム | 💤 **完全凍結**。v1.xでは触らず、使われてから需要に応じて再評価 |

---

## 凡例

| 記号 | 意味 |
|------|------|
| ✅ | 実装済み・動作確認完了 |
| 🚧 | 現在着手・進行中 |
| 🔲 | 未実装（次期フェーズ） |
| 💤 | 保留（Backlog・v1.xでは凍結） |
| 🌟 | ネオ秘書くん独自の差別化機能 |

---

## 1. タスク・TODO管理

> TickTickのコア機能。「やること」の作成・管理・完了追跡。

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 1.1 | タスクの作成（手帳UI & クイック入力） | ✅ | 手帳ウィンドウからEnterキー一発でタスク登録 |
| 1.2 | 🌟 自然言語でのタスク作成（AI経由） | ✅ | 「〜をタスクに追加して」→ AIが自動登録（`create_task_tool`） |
| 1.3 | サブタスク（チェックリスト） | 🔲 | 【コード実態監査】DB層（`tasks.parent_id`）未実装。PC手帳UI・スマホPWAともに未対応。将来のEpic/Subtask拡張として保留 |
| 1.4 | 優先度（高/中/低） | ✅ | 🔥高 / ⚡中 / ☕低 の優先度設定とバッジ表示 |
| 1.5 | タグ付け | ✅ | 自由なラベルでタスク分類 【2026-08-31 実装】task_parser による #タグ解析 + tasks.tags カラム + クイック追加バーのタグバッジ表示 |
| 1.6 | リスト（プロジェクト） | ✅ | 【2026-08-31 完了】DB層+AIツール・サーバーアクション＋スマホリストチップ＋**PC手帳TickTick風階層UI (Block 1.6-R)**: `task_lists.parent_id` 汎用ツリー（多階層・循環防止 `move_task_list`・削除時子昇格）＋左サイドバー折りたたみツリー（▶/▼・子孫合計件数バッジ）＋右メイン2分割。親選択時は子孫タスクも表示 |
| 1.7 | スマートリスト（フィルタ） | ✅ | 条件指定で自動分類 【コード実態監査 完了】スマホTODOモーダルに「⏰今日 / 📅今週」期間フィルタ＋🏷タグ絞り込み＋🎯4象限トグルが完全実装済み (`filterTasks`) |
| 1.8 | Inbox（受信トレイ） | ✅ | 未整理タスクの手帳一覧表示 |
| 1.9 | タスク検索 | 🔲 | キーワードでタスクを横断検索 |
| 1.10 | 一括編集 | 🔲 | 複数タスクのまとめて変更 |
| 1.11 | タスク完了・チェックボックス | ✅ | 手帳およびスマホPWAからワンタップで即時完了マーク |
| 1.12 | 繰り返しタスク | ✅ | 【2026-08-31 実装】「毎日/毎週月曜/毎月1日」構文解析 (task_parser) ＋ tasks.recurrence カラム＋完了時に次回分を自動生成 (database._next_recurrence_due、放置スキップ・月末クランプ対応)。スマホTODOに🔄バッジ表示 |
| 1.13 | メモ・添付ファイル | 🔲 | タスクへの詳細メモやファイル添付 |
| 1.14 | 🌟 タスク属性（重要度×緊急度）の明示保持 | ✅ | 【2026-08-31 実装】quick-add 構文 `※重要/※非重要/※緊急/※非緊急` 解析 (task_parser) ＋ tasks.importance_flag/urgency_flag カラム＋PC手帳セレクトUI (calendar_window) ＋スマホ quick_add 反映。7.9 4象限は明示属性最優先・未設定は従来ルールへフォールバック → 「全部後回し」問題を解消 |
| 1.15 | TODO UI/UX刷新 | ✅ | 【2026-08-31 実装】スマホTODOモーダルのフィルタ行をラベル付き2段化（📋 リスト / 🗓 期間+🎯4象限トグル / 🏷 タグ）。リスト未登録時はリスト行を非表示。ボス実機フィードバック 2026-08-31 に対応。第2弾: 🔥重要/⚡緊急バッジ・期限切れ赤字強調の常時表示＋完了を✓ボタン明示操作化＋誤タップ時は同一行タップで復元 (reopen_task) |

---

## 2. カレンダー・予定管理

> 手帳感覚で直感的に使えるスケジュール管理。

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 2.1 | 予定の作成 | ✅ | AI経由でタイトル・開始/終了時刻を指定して作成 |
| 2.2 | 直近の予定一覧表示 | ✅ | カレンダーウィンドウに手帳風で表示 |
| 2.3 | 月表示カレンダー (月間ビュー) | ✅ | ［🗓️ 月間 (30日)］ 手帳風の月間予定一覧表示 |
| 2.4 | 週表示カレンダー (週間ビュー) | ✅ | ［📅 週間 (7日)］ 直近1週間の予定集中表示 |
| 2.5 | 日表示カレンダー (日間ビュー) | ✅ | ［☀️ 日間 (今日)］ 今日の予定のみをピンポイント表示 |
| 2.6 | 年表示カレンダー | 🔲 | 年間俯瞰ビュー |
| 2.7 | 予定のドラッグ＆ドロップ | 🔲 | カレンダー上で予定を移動 |
| 2.8 | 終日予定 | 🔲 | 時間を持たない1日単位の予定 |
| 2.9 | 繰り返し予定 | 🚧 | 【2026-08-31 精査確認】DB層は対応済み (`create_event` の recurrence_type/recurrence_rule・ICS同期)。PC手帳の繰り返し入力UIが薄く本体の定期生成ロジックは要確認 |
| 2.10 | カテゴリ・色分け | 🔲 | 予定にカテゴリと色を設定 |
| 2.11 | Google Calendar / iCalendar (.ics) 同期 | ✅ | 標準.icsエクスポート ＆ 設定画面からのGoogle連携UI |
| 2.12 | カウントダウン | 🔲 | 重要イベントまでの残り日数表示 |
| 2.13 | タイムゾーン対応 | 🔲 | 海外の時間帯に対応 |
| 2.14 | 🌟 外部カレンダー・SaaSマルチ中継Webhook双方向連携 | ✅ | Google審査・100名制限を回避する疎結合ハブ。ユーザー自身のZapier/Make/IFTTT/GAS Webhook URLを登録し、Googleカレンダー・Notion・Slack・LINEへ予定・タスクを双方向同期 (2026-08-30 実装完了) |

---

## 3. リマインダー・通知

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 3.1 | 時刻ベースのリマインダー | ✅ | 【2026-08-31 実装】reminder_engine.py (30秒周期監視・期限10分前発火・reminders_sent冪等記録で二重通知防止)。PC吹き出し+スマホ /api/status due_reminders 通知 |
| 3.2 | 複数リマインダー | 🔲 | 1つの予定に複数のリマインダーを設定 |
| 3.3 | 🌟 AIによるプロアクティブ通知 | ✅ | 45分作業や夕方に自律的に休憩を促す (`proactive_engine.py`) |
| 3.4 | Windows システム通知連携 | 🔲 | OSのトースト通知と連動 |
| 3.5 | サウンド通知 | ✅ | 【コード実態監査 完了】PC側 `winsound.MessageBeep` ＋ スマホPWA Web Audio API チャイム（予定紫バナー連動・エージェント承認要請音）が完全実装済み |
| 3.6 | 🌟 予定直前紫バナー・チャイム・パルスバイブレーション通知 | ✅ | 【2026-09-06 実装】予定10分前および開始時刻にスマホPWAに `.reminder` 紫バナー、チャイム音、パルスバイブレーション、表情変化（alarm_ask）を即時発火。エージェント承認要請並みに強調して認知摩擦をゼロ化 |

---

## 4. 付箋（Sticky Notes）

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 4.1 | 付箋の作成（AI経由） | ✅ | AIに「メモして」と言うとデスクトップに付箋出現 |
| 4.2 | 付箋の直接編集 | ✅ | 付箋上のテキストを直接クリックして書き換え |
| 4.3 | 付箋の削除（DB連動） | ✅ | ✖ボタンでDBからも完全削除 |
| 4.4 | ドラッグ移動 | ✅ | ヘッダーをドラッグして画面上を自由に移動 |
| 4.5 | 色の変更 | 🔲 | 付箋ごとに色を選べる |
| 4.6 | リサイズ | 🔲 | 付箋の大きさを変更 |
| 4.7 | 最小化・復元 | 🔲 | 一時的に小さくたたむ |
| 4.8 | 重なり順序（z-index） | 🔲 | どの付箋が前面かを制御 |
| 4.9 | 移動位置の永続化 | ✅ | 【2026-08-31 精査確認】`_do_move` が position_x/y をDB保存し、起動時 geometry 復元 (`ui/sticky_note.py`) |

---

## 5. 習慣トラッカー（Habit Tracker）

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 5.1 | 習慣の作成 | ✅ | 【2026-08-31 精査確認】PC手帳習慣タブ (`_build_habits_tab`/`_on_add_habit`) ＋スマホ `add_habit` アクション。`create_habit` DB実装 |
| 5.2 | チェックイン（達成記録） | ✅ | 【2026-08-31 精査確認】PC (`_on_toggle_habit`) ＋スマホ (`toggle_habit` アクション) のワンタップ達成記録。`toggle_habit_log` DB実装 |
| 5.3 | 頻度設定 | ✅ | 【コード実態監査 完了】週N回 (`target_days_per_week`) カラム・ロジックがDBおよびUI層で完全実装済み |
| 5.4 | 連続達成（ストリーク）表示 | ✅ | 【2026-08-31 精査確認】`get_habits_with_status` が streak 計算 (今日未達なら昨日起算) ＋PC/スマホ両UI表示 |
| 5.5 | ヒートマップ | 🚧 | 【2026-08-31 精査確認】`get_habit_heatmap_data` (70日・レベル0-4) ＋PC手帳ヒートマップUI実装済み。スマホPWA表示は未実装 (payloadには含まれる) |
| 5.6 | 🌟 AIによる習慣アドバイス | 🚧 | 【2026-08-31 精査確認】AI生活コーチ (B23 Phase L1) の日次分析に習慣達成率が組み込み済み。個別習慣へのアドバイスは Phase L2 で強化 |

---

## 6. ポモドーロ・集中タイマー ＆ サポート

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 6.1 | ポモドーロタイマー (双方向同期) | ✅ | PCサークルメニュー ＆ スマホDeskPetヘッダーで25分集中+5分休憩 |
| 6.2 | 時間のカスタマイズ | 🚧 | 【2026-08-31 精査確認】サークルメニューに複数時間プリセット (`pomodoro_total_seconds = minutes * 60`)。設定画面からの自由入力は未実装 |
| 6.3 | ホワイトノイズ | 🔲 | 集中用BGM（雨音、カフェ等） |
| 6.4 | タスク連動 / 集中アニメーション | ✅ | 集中時にハチマキ着用アニメ（`focus_1/2`）連動 |
| 6.6 | 🌟 ヘルプ/使い方ガイド（設定画面内） | 🔲 | 初回起動オンボーディングツアー＋機能別ガイド。設定画面のタブとして実装 |
| 6.7 | ポモドーロの統計・記録 | 🔲 | 何時間集中したかの可視化 |
| 6.8 | ホワイトノイズ（集中用BGM） | 🔲 | 雨音、カフェ等の環境音 |

---

## 7. AIエージェント機能 🌟

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 7.1 | 自然言語による全操作 | ✅ | 「明日15時に会議」で予定登録等 |
| 7.2 | LangGraphによる自律推論 | ✅ | ツール呼び出しの自動判定 |
| 7.3 | 🌟 Multi-LLM Factory（動的切り替え） | ✅ | OpenCode GO (DeepSeek-Flash) / LM Studio / Gemini をサークルメニューから動的切替 |
| 7.4 | 会話の長期記憶（MemorySaver + 知識の宝庫） | ✅ | 過去の会話を覚えてユーザー知見（`user_insights`）を自動蓄積 |
| 7.5 | 🌟 Human-in-the-Loop（スマホ＆Bluetooth承認） | ✅ | Agent Bridge Hub によりスマホおよびBluetoothイヤホンからノールック承認 |
| 7.6 | 🌟 画面認識 (Vision / スクリーンショット解析) | ✅ | ショートカットやツール呼び出しで画面を撮影・解析 (`vision_tools.py`) |
| 7.7 | Web検索エージェント | ✅ | 【2026-08-31 精査確認】Playwright非使用だが `web_tools.py` (Google News RSS / Jina Search / Jina Reader) でゼロ設定Web検索を実現。9.13 ニュースサマリの基盤 |
| 7.8 | 朝の挨拶・日次ブリーフィング | ✅ | 【2026-08-31 精査確認】`briefing_engine.py` 4モード (morning/day/evening/night) — 天気・予定・TODO・習慣を統合した朝会/終礼レポート生成＋キャラ別セリフ。`tests/test_briefing_engine.py` 全パス |
| 7.9 | 🌟 アイゼンハワーマトリクス自動分類 | ✅ | 【2026-08-31 ルールベース実装完了】スマホTODOに🎯4象限ビュー。明示属性 (※重要/※緊急, 1.14) を最優先 → 未設定は優先度・期限で推定するフォールバック方式で「全部後回し」問題を解消。AI推定への拡張は後続オプション |
| 7.10 | 🌟 メール→タスク変換 | 🔲 | メール内容をAIが読み取りタスク化 |
| 7.11 | 🌟 思考＆応答ストリーミング表示 | ✅ | `agent_stream.py` 経由でLLMの思考過程と回答をリアルタイムSSE配信・UI描画 |
| 7.12 | 🌟 プロバイダ仕様自動追随＆ヒント | ✅ | OpenCode GOのセッション自動注入、エラー時の自己修復ヒント提示 |

---

## 8. デスクトップ常駐ペット＆マスコット 🌟

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 8.1 | 🌟 29種のドット絵スプライト (Pixel Art 2.0) | ✅ | レトロなドット絵キャラクターが画面隅に常駐 |
| 8.2 | 🌟 状態アニメーション＆生活モーション | ✅ | 待機、思考、歓喜、集中、お茶、読書、ストレッチ、夜更かし |
| 8.3 | 🌟 放射状サークルメニュー (Circle Menu) | ✅ | クリックで8方向に滑らか展開、ホバー説明付きクイック操作 |
| 8.4 | 🌟 リアルタイム視線追従 (Pupil Tracking) | ✅ | マウスカーソルの位置を目線が追いかける |
| 8.5 | 🌟 触感インタラクション (Hover / Petting) | ✅ | カーソル接近で目を細め、クリック/なでなでで喜ぶ |
| 8.6 | 🌟 徘徊モード（デスクトップ散歩） | ✅ | 【コード実態監査 完了】PC側 (`gui.py:_step_walk`) およびスマホPWA (`web_pet/pet.js:petWanderTick`) の双方で歩行・方向転換・反転・休憩が完全稼働。設定メニューでON/OFFトグル・永続化済み |
| 8.7 | 🌟 Windows タスクトレイ常駐マネージャー | ✅ | `pystray` + `PIL` によるバックグラウンド常駐、タスクバー非占有、右クリックメニュー（再表示・設定・終了）、×ボタン最小化連動 (`ui/system_tray.py`、2026-09-06 実装完了) |
| 8.8 | 🌟 CustomTkinter Tcl終了ハング三重防御 | ✅ | `ui/tk_teardown.py` による強制バインド解除・Tcl非同期安全シャットダウン |

---

## 9. スマートフォン専用ペット端末連携（Desk Pet Device / Bluetoothダイレクト） 🌟

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 9.1 | 🌟 ローカル同期サーバー基盤 (ポート8765) | ✅ | PC側でPWA配信 ＆ リアルタイム同期API提供（Bluetooth PAN / Wi-Fi対応） |
| 9.2 | 🌟 スマホ用ペット画面 (Desk Pet PWA) | ✅ | スマホブラウザで全画面表示するGameBoy風ドット絵ペット |
| 9.3 | 🌟 AIエージェント進捗中継＆承認 (Agent Bridge Hub) | ✅ | Claude Code, Codex, Antigravity等の承認要請をスマホにPush＆ワンタップ承認 |
| 9.4 | 🌟 🎧 Bluetoothイヤホン遠隔承認 (MediaSession API) | ✅ | 耳のイヤホンのボタンやスマホ物理音量キーでノールック承認 |
| 9.5 | 🌟 無限生配信ストリーム (NoSleep captureStream) | ✅ | Canvasアニメーションのライブ動画化によりOSスリープを100%確実に阻止 |
| 9.6 | 🌟 PC側ペット自動最小化（画面占有ゼロ化） | ✅ | スマホ接続時にPCペットを自動非表示(`withdraw`)し作業領域を最大化（トグル設定可） |
| 9.7 | 🌟 スマホからのPCペット遠隔呼び出しボタン | ✅ | スマホ画面からPCペットを即座に再表示(deiconify) |
| 9.8 | 🌟 特大デジタル時計 ＆ ポモドーロヘッダー | ✅ | スマホ画面上部に視認性の高い大型時計と集中タイマーバッジ |
| 9.9 | 🌟 インテリジェント・サジェストカード | ✅ | 予定・Slack・関心ニュースを先回りサジェスト（ソース別ON/OFF対応） |
| 9.10 | 🌟 オンデマンド手帳・ボトムアクションバー | ✅ | 予定一覧・TODO（タップ完了）・習慣（タップ達成・ストリーク表示）・設定モーダルを実装 (2026-08-25) |
| 9.11 | 🌟 Tailscale HTTPS/HTTP 両対応 QR 接続 | ✅ | Tailscale Serve の有無に応じ HTTPS（全機能）/ HTTP（設定不要・手軽）を選択可能。Tailscale IP (100.64/10) を ipconfig から CP932 対応で自動検出。tailscale serve コマンドのコピーボタン搭載 (2026-08-28) |
| 9.12 | 🌟 ペット生命感強化（PWA歩行＆セリフミラー） | ✅ | PWA: idle_1/2 フレーム切替＋歩行AI（左右ランダム歩行・方向転換反転・特別状態で中断）。セリフ: /api/status に PC ペット最新セリフを同期（発話ミラー）＋キャラ別挨拶×時間帯×90秒ローテーションで固定文言解消。歩行は SW キャッシュ v4.5 化＋walk フレーム未保有キャラの idle 代用＋TDZ 解消で全キャラ対応 (2026-08-29) |
| 9.13 | 🌟 関心ニュースキーワード設定 ＆ 3行AIサマリ | ✅ | 登録不要・完全無料の検索基盤（Google News RSS / DuckDuckGo / Jina）＋設定画面での関心キーワード自由設定＋LLMFactoryによる超軽量3行AIサマリ生成（AIオフ時ルールベースフォールバック） (2026-08-30 実装完了) |
| 9.14 | 🌟 通知エージェント自動識別 | ✅ | 環境変数から Cline / Claude Code / Cursor / Codex / Antigravity / Gemini CLI を自動判定し、承認通知にアイコン・名称を表示 (`agent_identity.py` + `tests/test_agent_identity.py`、2026-08-31 精査確認) |
| 9.15 | 🌟 ゼロトラスト端末台帳＆トークン個別失効 | ✅ | SQLite `devices` テーブル、Pydantic `Device`、SHA-256トークンハッシュ照合、`/api/devices` 一覧 ＆ `/api/devices/revoke` 即時失効 (`database.py`, `local_sync_server.py`、2026-09-06 実装完了) |
| 9.16 | 🌟 自己治癒ウォッチドッグの独立Seam分離 | ✅ | `server_watchdog.py` 独立モジュール化、連続2回失敗ヒステリシス、指数バックオフ再起動、終端契約 (`server_watchdog.py`, `tests/test_server_watchdog_standalone.py`、2026-09-06 実装完了) |
| 9.17 | 🌟 認証失敗IPレートリミッター | ✅ | 401 Unauthorized 連続失敗検知、指数ペナルティ、DoS/ブルートフォース防御 (`auth_rate_limiter.py`、2026-09-06 実装完了) |

---

## 10. 開発の優先度とロードマップ
 
 ### 🔴 完了済み (Phases A〜E)
 - **Phase A〜E: デスクトップ常駐MVP・LangGraph・知識の宝庫・手帳・PWA・MCP自動登録 [✅ 完了]**
   - 29種ドット絵ペット、1-bit Alpha透過、マルチLLM動的切替、SQLite WAL/7世代自動バックアップ、PWA NoSleep生配信、Bluetoothイヤホン遠隔承認、MCPワンクリックインストーラー。
- **Phase F: ゲーム級UX ＆ アニメーション基盤 [✅ 完了 2026-08-31]** — ポモドーロ円形アーク (`_draw_pomodoro_arc`) ＆ EffectOverlay 集中演出。
- **⚡ パフォーマンス短期改善 Step 1-3 [✅ 完了 2026-08-31]** — `ui_notify.py` 即時更新ブリッジ / サジェストBGワーカー化 (GIL競合解消) / マルチLLM検出並列化 (docs/specs/パフォーマンス短期改善設計書_20260830.md 参照)。
- **🎮 秘密の部屋 レトロミニゲームセンター（オムニバス5種）[✅ 完了]** — `minigame_arcade.js` カートリッジ方式 (インベーダー/糸通し/電脳イライラ棒/刹那の見斬り/ブロックラリー) ＋ ハイスコア永続化API (`/api/minigame/high` ＋ `record_minigame_score`)。旧 `pixel_defense.js` はエイリアス互換維持。
 
 ---
 
 ### ✅ Phase F: ゲーム級UX ＆ アニメーション基盤 [✅ 完了 2026-08-31 精査確認]
 1. **[Phase F-1] ポモドーロの円形ネオンプログレスバー (`gui.py`)**:
    - 残り時間に応じて青➔緑➔橙➔赤へと滑らかに色が変化する Canvas アークゲージ ＆ デジタルタイマー。**実装完了**: `gui.py` `_draw_pomodoro_arc`。
 2. **[Phase F-2] 集中時の闘気・炎エフェクト ＆ 猛烈タイピング演出 (`pet_animator.py` & `gui.py`)**:
    - キャラクター背後へのオーラ・炎（🔥）・集中線透過レイヤー合成（`EffectOverlay`）。**実装完了**: `pet_animator.py` EffectOverlay ('flame' / 'focus_aura' / 'sparks' / 'sweat')。

---

### ✅ 完了 (2026-08-31 スプリント)
1. ~~**TODO UI/UX刷新 (1.15)**~~ ✅ フィルタ行ラベル付き2段化・4象限トグル集約。
2. ~~**タスク属性入力 (1.14)**~~ ✅ ※重要/※緊急 構文＋DB属性カラム＋PC/スマホUI・4象限フォールバック。
3. ~~**繰り返しタスク (1.12)**~~ ✅ 毎日/毎週/毎月解析＋完了時次回自動生成。

---

### ✅ 完了 (2026-09-06 集中開発4サイクルスプリント)
1. ~~**サイクル1: 音声入力UIトグル化 ＆ 予定リマインダー紫バナー**~~ ✅ `ui/settings_window.py` 音声入力ON/OFFトグル、`database.py` `voice_input_enabled` 設定永続化、`web_pet/pet.js` 予定リマインダー紫バナー演出 (`event_reminder`)。
2. ~~**サイクル2: Windows タスクトレイ常駐マネージャー**~~ ✅ `ui/system_tray.py` (`pystray` + `PIL`) バックグラウンド常駐、タスクバー非占有、右クリックメニュー（再表示・設定・終了）、×ボタン最小化連動。
3. ~~**サイクル3: ゼロトラスト接続端末台帳 ＆ 個別失効API**~~ ✅ SQLite `devices` テーブル、SHA-256トークンハッシュ照合、`_check_auth` 失効403遮断＆Fail-Safe `touch_device_last_seen`、`/api/devices` GET ＆ `/api/devices/revoke` POST ループバック限定。
4. ~~**サイクル4: 自己治癒ウォッチドッグの独立Seam分離**~~ ✅ `server_watchdog.py` 独立モジュール化、連続2回失敗ヒステリシス、指数バックオフ再起動、終端契約。単体テスト全パス。

---

### ✅ 完了 (2026-09-18 ゼロトラスト個別トークン化 ＆ P0/P1是正スプリント)
1. ~~**端末台帳のゼロトラスト個別トークン化 ＆ 復帰API**~~ ✅ `issue_device_token` / `verify_device_token` によるSHA-256ハッシュ認証、PC手帳設定画面「📱 接続端末管理」タブの復帰ボタン（un-revoke）および監査ログ書き込み。
2. ~~**ゼロトラストP0脆弱性3件の即時封鎖**~~ ✅
   - P0-1: スマホ全解除（マスタトークン再生成）時に `database.revoke_all_devices()` を自律同期実行し個別端末を一括失効。
   - P0-2: QRペアリング時の個別トークン正常発行直後に `close_pairing()` を呼びワンタイム化（同一LANからの無制限発行DoS遮断）。
   - P0-3: `DeskPetSyncHandler.timeout = 10.0` によるSlowlorisソケットハング・スレッド枯渇の完全防止。
3. ~~**品質官P1バグ修正 ＆ ゼロトラストFail-Closed徹底**~~ ✅
   - P1-1: 終礼日報で過去全期間の完了タスクが出力されるバグを根治（`get_tasks_completed_today` 新設・本日00:00以降の完了分のみ抽出）。
   - P1-2: QRペアリング例外時のマスタトークン漏洩フォールバックを完全廃止し HTTP 500 返却（Fail-Closed徹底）。
   - P1-3: `GET /api/devices` 端末一覧取得をループバック（同一PC）限定に遮断しLAN内覗き見を防止。
4. ~~**ループバック台帳完全除外 ＆ 403自動リカバリ ＆ QRライフサイクル連動**~~ ✅
   - PC内部通信（127.0.0.1）を台帳登録から完全除外・既存ゴミレコードを一掃する `cleanup_loopback_devices` 新設。
   - `pet_auth.js` が 403 受信時にも無効トークンをクリアして自動再取得を試行するフォールトトレラント化。
   - トークン発行直後のゼロ秒締め出しを撤廃し、QRダイアログを閉じた時（`destroy`）に `close_pairing` を呼ぶ直感ライフサイクルへ是正。

---

### 🔵 次回スプリント (Sprint C: GitHub Actions CI/CD ＆ 公開踏切 ＆ Human-in-the-Loopペアリング)
- **🌟 PCペット側でのスマホ接続要求・承認ダイアログ (Human-in-the-Loop Pairing)**: スマホからの接続要求をPCペット側で可視化・把握し、承認ダイアログでユーザーが許可して永続トークンを発行する世界基準のゼロトラストUI。
- **CI/CD自動化**: `.github/workflows/ci.yml` による push / PR 時の自動テスト、型チェック、`scan_git_secrets.py` 実行。
- **設定画面 端末一覧UI (Mini-Sprint)**: `ui/settings_window.py` への接続端末一覧表示およびワンクリック失効（Revoke）ボタン。
- **Cap プロモ動画撮影**: `Cap` (cap.so) を用い、PCペットとスマホDesk Petが連動する「30秒製品プロモ動画」を撮影し、READMEトップおよびX告知用に配備。

### 🔵 次期フェーズ (Phase G & H: MiniCPM-Pet進化系ハイブリッド統合)
3. **[Phase G] 内包型ローカルLLM推論基盤 ＆ モデル管理 (Tier 1 GGUF / llama-server)**:
   - `llama.cpp` サイドカー管理、MiniCPM5-1B / LFM2.5 / Bonsai / Qwen プリセットDL ＆ 任意GGUFドラッグ＆ドロップ。
4. **[Phase H] フルハイブリッドAgent監視 ＆ 状態連動FSM ＆ タスクナレーション**:
   - Claude Code / Cursor / Codex ログ監視 (File Watcher) ➔ 状態連動 (Thinking/Coding/Waiting/Success) ➔ 1行完了要約発話。
5. **[Phase I] 自作キャラクター・スキン取り込み基盤 (Custom Pet Skins / Modding) ＆ キャラクタースキンシステム**:
   - 4大キャラ（キノコ君・アザラシ君・マーモット等）の正式復活 ＆ GitHub草風ヒートマップ。
   - **🌟 自作キャラ画像取り込み (Modding Support)**: `assets/custom_pets/<char_id>/` に画像（idle/cheer/celebrate等）と `character.json`（設定・プロンプト）を置くだけで、ユーザー自身のオリジナルキャラ・推しキャラをPCペット＆スマホDesk Petとして動かせる自由拡張フレームワーク。
6. **[Phase M] フル英語・多言語対応 (Full i18n Support)**:
   - `i18n.py` 基盤を UI・PWA・セリフへ完全接続。ワンタップでの日英言語切り替え、英語圏ユーザー向けオンボーディングツアー。
6. **[Phase J] 配布パッケージング（GitHub公開 ＆ exe化 ハイブリッド） [🚧 v1.0.0ビルド完成・手渡し配布開始 2026-08-31]**:
    - 方式: **ハイブリッド**（GitHub公開 + PyInstaller onedir）。✅ 2026-08-31: `dist/NeoHisho_v1.0.0_win64.zip` (60.6MB) ビルド完成・機密スキャンCLEAN・UPDATE_GUIDE同梱。**手渡し配布開始** (リポジトリ非公開維持)。残: git履歴機密監査→README→tag push (タグv1.0.0ローカル作成済み)→Release作成。
    - LLMモデル: ランタイム非同梱。✅ `NeoHisho.exe --setup-model 350m|1.2b` でHugging Faceから自動DL (実装済み)。
    - README: スクリーンショット＋バッジ＋機能一覧で充実。Cap (cap.so) による自動ズーム＆滑らかパンの30秒プロモ動画（PCペット×スマホ連動）をトップ掲載。
    - ヘルプ/オンボーディング: 設定画面に「使い方ガイド」タブ追加（初回起動ツアー含む）。
    - exe化: PyInstaller onedir (neo_hisho.spec確定・faster-whisper等は内部課題化に伴い配布除外)。コード署名証明書は検討。
 7. **[Phase L] バズ・相棒MVP（マスタードキュメント反映 2026-08-28） [🚧 主要実装完了 — 演出チューニング等の仕上げのみ残]**:
    - Week1: retro_dolphin仕様決め＋スプライト初版＋キャラ切替追加 / 「お前を消す方法」入力検知 / レベル1・2演出
    - Week2: グリッチ復活演出 / 朝会・終礼テキスト設計＋日次導線 / pixel_defenseプロトタイプ＋条件解放接続
    - 詳細仕様: docs/specs/秘書くんマスタードキュメント.md および本表 11章 / docs/specs/DESIGN_SPEC.md 9章

---

## 11. バズ・相棒MVP（Phase L） 🌟

> 出典: 2026-08-28 Genspark壁打ち「秘書くん 完全版マスタードキュメント v1.0」(docs/specs/秘書くんマスタードキュメント.md)。
> 判断基準は「初見3秒で好きになるフック」「7日後も机の上に残る理由」の2軸。この2つに効かない機能は本フェーズへ入れない。

| # | 機能 | 状態 | 説明 |
|---|------|------|------|
| 11.1 | 🌟 レトロ案内精霊キャラ (retro_dolphin) | ✅ | 【L0 スプライトv2(側面)生成完了・L1 キャラ統合完了(CHARACTERS_DATA/PC・PWA切替/29種スプライト展開)】イルカ型Officeアシスタント文脈のオマージュ（複製はしない）。`character_manager.py` の `CHARACTERS_DATA` へ追加。スプライトは idle / appear / offended / revive の4状態（`assets/retro_dolphin/`） | 【追記 2026-08-29: カイル強寄せスキン(id: kyle)追加 — ラベンダー配色・直視目・歩行フレーム含む31種/キャラ(ホタテ貝型PCは視認性不良により削除)。retro_dolphin は温存】
| 11.2 | 🌟 「お前を消す方法」イベント | ✅ | 削除系ワード（お前を消す方法 / 消し方 / 邪魔 / 消えて）を検知して発火。1日1回目=軽反応、3回目=重演出、5回目=ミニゲーム解放。発火回数は永続化。【2026-08-28 実装】PC側検知エンジン easter_egg_engine.py（フレーズ主判定＋ホワイトリスト）＋ LLM 文脈応答化（発火文脈を LLM へ注入、固定台詞はフォールバックに降格）＋ テスト7件全パス。PWA 側演出は後続 |
| 11.3 | 🌟 段階演出エンジン | ✅ | 【2026-08-31 精査確認】`web_pet/easter_eggs.js` — 暗転・CRT走査線・RGB色収差グリッチ・ピクセル粒子消滅・復活演出＋Stage1-4の8bitシンセ効果音＋低スペ端末自動判定をWeb Pet PWA側に実装済み。元仕様: 暗転300ms→キャラ中央移動→0.4秒の溜め→放射パーティクル2段→3フレームグリッチ→0.8秒で再構築→復活台詞。低スペ端末向け軽量化＋スキップ設定を必須化 |
| 11.4 | 🌟 朝会 / 終礼モード | ✅ | 【2026-08-31 精査確認】7.8 日次ブリーフィングと統合実装済み (`briefing_engine.py` 4モード・朝/昼/夕/夜の時間帯自動判定)。`tests/test_briefing_engine.py` 全パス。元仕様: 朝: 今日の予定確認＋タスク3件整理。夜: やったことの振り返り＋承認処理件数。1分以内で見終わる軽さ。7.8 日次ブリーフィングと統合実装 |
| 11.5 | 🌟 インベーダー風ミニゲーム (pixel_defense) | ✅ | 【2026-08-31 精査確認】単発から**オムニバス5種へ進化** — `minigame_arcade.js` カートリッジ方式 (インベーダー/糸通し/電脳イライラ棒/刹那の見斬り/ブロックラリー) ＋ ガチャ起動 ＋ ハイスコア永続化API。旧 `pixel_defense.js` はエイリアス互換維持。元仕様: 条件達成でのみ解放されるご褒美機能。`web_pet/minigame_pixel_defense.js` として本体と疎結合・削除可能構造。5分未満。スコアランキング等は作らない |
| 11.6 | 音声入力導線（Web Speech実装済み） | 🚧 | 【2026-08-31】音声入力は品質向上のため内部課題化 (VOICE_INPUT_ENABLED=false・Whisper経路は資産温存)。解放条件: 実機品質合格＋キャラエリア大型マイクボタン移設。元記述: 【2026-08-31 精査確認】スマホPWAに `startVoiceInput` (Web Speech API音声認識) ＆ `speakText` (TTS発話) 実装済み (`pet.js`)。PC側Whisper本実装は B20 へ。元仕様: 「話しかける」ボタン配置のみ。録音はスマホ、Whisper等の文字起こしはPC側で処理するPC本拠地主義を固定。本実装は B20 |
| 11.7 | Google予定追加UI地ならし | 🔲 | 「将来解放される能力」としてのロック表示。BYO認証（ユーザー自身のClient ID）前提のオンボーディング導線設計。本実装は B19 |
| 11.8 | 機能解放UI（ロック表示） | 🔲 | 未解放機能は「ロック」表示で存在を伝える。「3分で設定できます」形式の導線。Client ID読み込み・Whisper有効化はPC側設定画面から |
| 11.9 | 🌟 新生5大キャラクタースイート（アザラシ・キノコ全面改修＆絶叫マーモット新設） | 🔲 | 【2026-08-31 ボス承認・ゼロベース再設計】全5大キャラ（秘書くん/カイル/もちもちアザラシ/メガネ書記官キノコ/絶叫悟りマーモット）のアセット生成・統合。アザラシは大福フォルム＋`tea_pillar`（茶柱ぷかぷか）、キノコはメガネ＋羽ペン＋`writing`（タスク記録時メモ）、**マーモット(marmot)は新規追加**（`screaming` 絶叫＝リマインダー接近・遅延時 / `tea`・`chill` 葉っぱくわえ / `petting` ツンデレ溶け）。`character_manager.py` へ marmot 追加・`tools/generate_kawaii_sprites_v2.py` 新設・`assets/dot/{seal,kinoko,marmot}/` 配備・PC/スマホ切替UI反映。詳細仕様は `docs/handover/prompt_for_cline_20260831.md` 参照 |

### Phase L 実装タッチファイル
- `web_pet/index.html` / `web_pet/pet.js` / `web_pet/easter_eggs.js`（新規） / `web_pet/minigame_pixel_defense.js`（新規）
- `character_manager.py`（CHARACTERS_DATA追加・既存キャラは hisho / kinoko / seal） / `ui/settings_window.py` / `database.py`（発火回数などの永続化） / `assets/retro_dolphin/`（新規）
- `tests/`（トリガー一致・新UI状態のスモークテスト追加）

### 直近Non-Goals（マスタードキュメントより）
- 多言語展開（日本語で完成度を優先） / ミニゲーム複数本展開（1本だけ） / 全部入り化 / Google OAuth本実装の全面公開 / ネイティブアプリ化

---

### 🔍 Phase L 実装レビュー決定事項（2026-08-28 レビュー採用）

- **① トリガー検知の誤爆対策**: 「お前を消す方法 / 消し方」等のフレーズ一致を主判定とし、「邪魔 / 消えて」の単語一致は補助スコアに格下げ。タスク・予定・付箋の正当な削除コマンド（例: 「タスクを消して」）を除外する**操作意図ホワイトリスト**を導入
- **② L0: アセット生成スプリントを新設**: L1より先に `docs/CHARACTER_PROMPTS_SPEC.md` + `tools/` の生成スクリプトで retro_dolphin スプライト品質を検証。合格（4状態・既存ドット絵とテイスト整合）してから L1 本体に着手（ドット絵背景廃止の前例を踏まえたリスク先行解消）
- **③ 発火カウンタの明文化**: 累積 `easter_delete_attempt_count`（5回でミニゲーム解放）と日次 `easter_delete_daily_count`（1回目=軽反応 / 3回目=重演出、`last_easter_trigger_at` で日跨ぎリセット）の**2本立て**
- **④ Phase L 成功基準を定義**: 30秒デモ動画が撮れること / 消す方法イベントを見た人から「何これ好き」の反応を得ること / スマホが7日間机上設置され続けること（手動観測）
- **⑤ リリース順序は次回ボス判断**: 案A = Phase M（v1.0.0）先行公開→Phase L を v1.1 バズアップデートに / 案B = L最優先3機能を v1.0.0 に内包して公開

---
## 12. 将来バックログ (B01〜B22) 🗄️

> 出典: マスタードキュメント「将来バックログ一覧 / 詳細」。**保存対象であり、明示指示なしに現行スプリントへ混入させないこと。**
> 再評価の順序: 相棒UX → リテンション → 作品性 → パワー機能。

| ID | アイデア | 分類 | 期待値 | 状態 |
|---|---|---|---|:---:|
| B01 | 多言語対応（英中韓独仏・東南アジア） | 展開 | 高 | 🔲 |
| B02 | ICU/BCP47ベース i18n 基盤 | 技術基盤 | 高 | 🔲 |
| B03 | イルカ系オマージュキャラの複数派生 | 世界観 | 中 | 💤 保留 |
| B04 | グリッチ闇落ち演出の拡張版 | バズ | 高 | 💤 現状維持 |
| B05 | 裏人格モードの本格実装 | 相棒 | 高 | 💤 保留 |
| B06 | 珍現象図鑑 / 実績解除 | 継続利用 | 中〜高 | 💤 保留 |
| B07 | 部屋の成長 / デスクルーム進化 | 相棒 | 高 | 💤 保留 |
| B08 | 信頼度 / 相棒度システム | 相棒 | 高 | 💤 保留 |
| B09 | ライフシアター（一日の物語化） | 演出 | 高 | 💤 保留 |
| B10 | 生活する秘書くん（時間・曜日・天候で変化） | 相棒 | 高 | 💤 保留 |
| B11 | マルチキャラ役割分担 | UX | 中〜高 | 💤 保留 |
| B12 | タイムガーデン（努力が景色になる） | 作品性 | 最高 | 💤 保留 |
| B13 | 秘書くん図書館（メモ・気づきの保管） | 知識OS | 高 | 💤 保留 |
| B14 | エージェントシアター（裏で働く可視化） | 可観測性 | 高 | 🔲 次期 |
| B15 | 承認前ナビゲーター（承認内容の翻訳） | 実用 | 高 | 🔲 次期 |
| B16 | 仕事の気配メーター | 相棒/可視化 | 高 | 💤 保留 |
| B17 | ひとこと付箋秘書 | 実用 | 中〜高 | 💤 保留 |
| B18 | ミニゲーム複数展開 | バズ | 中〜高 | 💤 凍結 (現状維持) |
| B19 | Google OAuth BYO認証の本実装 (双方向同期) | パワー機能 | 最高 | 💤 凍結 (iCal読み込みのみ維持) |
| B20 | スマホ音声入力 → PC Whisper本実装 (faster-whisper) | パワー機能 | 最高 | 💤 凍結 (OS標準優位・v1.x除外) |
| B23 | AI生活変化エンジン (Phase L2/L3) | 差別化機能 | 高 | 💤 凍結 (現行L1で完成形・保留) |
| B21 | Gmail / 外部SaaS拡張 (LINE/Notion双方向) | パワー機能 | 高 | 💤 凍結 (使われてから再評価) |
| B22 | ネイティブアプリ化（主にAndroid） | 配布/常駐 | 高 | 💤 凍結 (PWAで十分) |

---

## 🔄 2026-09-06 ロードマップ改訂 — Antigravity 最終監査 実測反映

- **更新日時**: 2026-09-06 18:30 (更新内容: 最終監査の指摘を実コードで相互検証し、バックログを再優先付け)
- **相互検証原則**: レビューの指摘も未検証のハルシネーションになり得る。採用判断は実コード実測で下す (審美眼と同じ原則をレビューにも適用)。

### 改訂バックログ (引き算ファースト)
1. 🟢 **完成: トークンハッシュの Deep Module 化** — `database.sync_device_session()` による暗号隠蔽。
2. 🟡 **P1-A 引き算: 露出制御 (Showcase / Minimal Mode)** — 新機能追加ではなく既存機能の非表示制御。
3. 🟡 **P1-B God Class 段階分離** — 残る do_GET 内の静的配信・台帳 GET・SSE 配信を api_*.py シーム群へ段階移管。
4. ✅ **P1-C 完了＆方針転換 (2026-09-06 20:00, Cline審査)**: GPU推論は外部（LM Studio/Ollama）委譲を基本とし、本体は超軽量キープ。
5. 🟢 **P2-R1 TLS (HTTPS) 化** — 社外展開時必須。現状は Tailscale 暗号トンネルで緩和済み。
6. 🟢 **P3 デザイントーン統一** — CustomTkinter × 8bit レトロの境界整理。

---

## 13. v1.1.0 承認リモコン特化 ＆ グローバル展開ロードマップ (2026-09-13 策定) 🚀

> **開発思想（Grilling合意）**: 「**席を外してもAIコーディングが止まらない**」という主役（承認リモコン）を極限まで尖らせる。機能の百貨店化を完全に戒め、外見の正常化・配布の成立・失敗モードの堅牢化・グローバル訴求を最優先とする。

### 13.1 ⚡ P0: 外見・配布の正常化 ＆ 世界への入口（即時着手）
- **状態**: 🚧 即時実行中
- **アクション項目**:
  1. **GitHub About（リポジトリ説明文）の正常化**:
     - 旧: `javascript AI秘書ペット...`（実装と矛盾）
     - 新: `Remote Approval Companion & Desktop Pet for AI Coding Agents (Claude Code, Cline, Cursor)`（5秒で修正）
  2. **GitHub Release v1.0.0 の正式公開**:
     - 最新コード（OpenCode 200・トレイ終了三重防御・ストリーミング・ゼロトラスト）で `build_exe.py` を再ビルドし、Releaseに添付。
     - アプリ内 `update_checker.py` の空振りを根治し、「日付のない野良コード」から「正式リリースされたOSS」へ昇格。
  3. **英語READMEの整備 (`README_en.md` ＆ 冒頭英語フック)**:
     - 冒頭5秒で痛点を射抜く: *"Approve your AI coding agent from your phone while taking a coffee break."*
     - 実エージェント2種（Claude Code / Cline）のコピペ3ステップ導入ガイドを英語で掲載。
  4. **30秒デモ動画（実写×Artemisハイブリッド）＆ Qiita公開**:
     - **実写（生活感）**: PCでエージェントが止まる ➔ 席を外してコーヒー ➔ スマホ画面を「人間の指がポンとワンタップ承認する神の一手」を撮影。
     - **Google Artemis（自動録画＆QA）**: PWA機能ラッシュ（TODO・部屋）の美しいキャプチャを自動収集。同時に「PWAフロントをAIで実機自動テストしている」技術的信頼を獲得。
     - 完成動画（または承認GIF）を埋め込み、GitHub Releaseがダウンロードできる万全の状態でQiita記事を公開。

### 13.2 🛡️ コア強化: Agent Approval Platform への昇格 (P0 / v1.1.0 重点項目)
- **状態**: 🔲 v1.1.0 スプリント最優先項目
- **課題 (Why)**:
  - 3者レビュー共通指摘（特にChatGPT・Grok）: 単一のMCP通知から脱却し、コマンドの安全性を保証するガバナンスと、複数エージェントを束ねる共通規格が不可欠。
- **改善方針 (What)**:
  1. **Agent Approval Protocol の抽象化 (Agent Adapter - P0)**:
     - 共通DTO `AgentApprovalRequest` を定義し、Claude Code / Cline / Codex / Cursor / Antigravity の差異をSeamで吸収。
     - 「単一ツール連携」から「AI Agent承認プラットフォーム」へ昇格。
  2. **コマンド実行 Policy (Approval Policy - P0)**:
     - 危険度に応じた3段階ポリシーエンジン：
       - 🟢 **Auto-Allow**: `npm test`, `git status`, 読取専用コマンドは自動即時許可（認知負荷激減）
       - 🟡 **Prompt**: `npm install`, ファイル編集, `git commit` はスマホワンタップ承認
       - 🔴 **Strict / Block**: `rm -rf`, `git push --force`, 秘密鍵漏洩リスクは赤色警告＆二重確認
  3. **Audit Log（承認監査ログ基盤 - P0)**:
     - いつ、どのエージェントが、どのコマンドを要求し、人間がどう判定したかをSQLite `approval_audit_logs` に記録。
  4. **承認の失敗モード明文化＆テスト (P0)**:
     - タイムアウト（エージェント側が諦めた後のスマホ操作の無効化）
     - ネットワーク切断・Wi-Fiローミング時の再送・冪等性
     - PWAバックグラウンド（画面消灯・スリープ復帰）時のWebSocket/SSE再接続
  5. **Google Artemis による PWAフロント実機自動テスト (P1)**:
     - `pet.js` (133KB) / `index.html` (49KB) のタッチ・承認・キャッシュ更新を自然言語シナリオで実機自動検証。

### 13.3 🌐 グローバル展開・マルチプラットフォーム基盤 (P1 / v1.1.0〜v2.0)
- **状態**: 🔲 v1.1.0〜v2.0
- **項目**:
  1. **フル多言語・英語UI切替 (Full i18n - P1)**: `i18n.py` 基盤を UI・PWA・セリフへ完全接続。ワンタップでの日英言語切り替え。
  2. **Mac / Linux クロスプラットフォーム対応 (P1 / v2.0 ADR)**: Tauri (Rust+Web) または pywebview による透過ウィンドウ近代化とマルチOS配布。

### 13.4 🎨 世界観: 自作キャラクター画像取り込み基盤 (Custom Pet Skins / Modding - P1)
- **状態**: 🔲 v1.1.0 スプリント優先項目
- **概要 (What)**:
  - 開発者が自力でドット絵を描き続ける泥沼を回避し、ユーザーやコミュニティが「自分の推しキャラ」を自由に配置して動かせる最小Mod機構。
- **仕様方針**:
  - `assets/custom_pets/<chara_id>/` に `idle_1.png` などの規約ファイルを置くだけで、`character_manager.py` が自動検知・登録。
  - 公式キャラは「秘書くん / カイル」の2体に固定し、追加キャラ量産はコミュニティへ開放。

### 13.5 🏗️ リポジトリ構造の近代化・パッケージ化（ルート直下の整理整頓 - P1）
- **状態**: 🔲 v1.1.0 スプリント対応
- **概要 (What)**:
  - ルート直下に平置きされた20本以上のモジュールを、標準的なパッケージ構造（`api/`, `core/`, `agents/`, `engines/` 等）へ整理し、GitHubトップページの第一印象を「洗練されたプロフェッショナルなOSS」へ格上げする。

### 13.6 🖥️ デスクトップGUI基盤の近代化評価（Grok指摘: Tkinterの限界への対応パス - P1 / v2.0）
- **状態**: 🔲 v2.0 ロードマップ（設計決定ADR記録済み）
- **背景と課題 (Why)**:
  - **Grokレビュー指摘**: TkinterはPython標準で軽量（60MB）かつWindows透過が容易な反面、Mac/Linuxクロスプラットフォームでの透過ウィンドウ制御の差異や、CSS・スプリング物理演算等のモダンWeb UI表現に限界がある。
- **アーキテクチャ対応方針 (What)**:
  1. **短期（v1.x: 役割の引き算）**:
     - PCデスクトップ側のTkinter画面は「最小限の透過キャラ常駐・通知・トレイ」に徹し、リッチな操作画面（TODO・習慣・部屋）はすべて**スマホPWA（Web標準）に逃がす**。スマホ接続時はPC側ペットを自動非表示（`withdraw`）とするため、Tkinterの限界がユーザー体験を阻害しない。
  2. **中長期（v2.0: 重厚Electronを避けた移行パス）**:
     - 150MB超に肥大化するElectronへの全面書換（死の罠）は避け、Rust+Web技術で超軽量・高速な **Tauri**、またはPython資産をそのまま活かせる **pywebview** を評価。
     - スマホPWAとして開発済みのフロントエンド資産（HTML/CSS/JS）をそのままデスクトップ画面としてラップ再利用できる設計を維持する。

### 13.7 🏛️ Codebase Design: モジュール肥大化解消 ＆ Seam（接合点）分割リファクタリング (P1 / v1.1.0)
- **状態**: 🔲 v1.1.0 スプリント最優先項目（2026-09-13 設計合意済み）
- **背景と課題 (Why)**:
  - 総合コードレビュー（deep-audit / ruthless-code-evaluation / codebase-design）により、`web_pet/pet.js` (3,335行) と `database.py` (2,498行) の2大モジュールが「God Object / 複数責務の同居」に陥っていることが判明。
  - 1つのファイルに複数のライフサイクル（通信・描画・物理演算・状態遷移）が混在しているため、「修正時の玉突き事故（副作用）」「AI推論オーバーヘッドによるハング」「隙間時間5分でのレビュー困難」を引き起こす真因となっている。
- **アーキテクチャ対応方針 (What)**:
   1. **PWAフロントエンド Seam 4分割 (P1 / v1.1.0)**:
      - **第1弾: `web_pet/pet_auth.js` 切り出し (✅ 2026-09-14 完了)**:
        - 認証・Bearer Token管理・401自己治癒・`authFetch` を独立Seam化。URLクエリの即時消去（画面共有時の漏洩防止）および同一オリジン限定ガードを完備。独立検証者 (`quality-reviewer`) による多角査読で **APPROVED** 獲得。単体テスト `tests/test_pet_seam_auth.py` 完備。
      - **第2弾: `web_pet/pet_audio_se.js` 切り出し (✅ 2026-09-14 完了)**:
        - Web Audio API による完全ローカル効果音合成（オシレーター/周波数ランプ/多重User Gesture解錠/キャラ別SE）を独立Seam化。未知キャラフォールバック完備。独立検証者 (`quality-reviewer`) による多角査読で **APPROVED** 獲得。単体テスト `tests/test_pet_seam_audio.py` 完備。
      - **第3弾: `web_pet/pet_particles.js` 切り出し (✅ 2026-09-14 完了)**:
         - 5大背景テーマシーン描画、天候・なでなでパーティクル、歓喜セレブレーション紙吹雪物理（放物線重力）、不滅アニメーションループ（try-finally保護）、双方向状態同期（`Object.defineProperty`）を独立Seam化。独立検証者 (`quality-reviewer`) 指摘のP0〜P3を完全反映。単体テスト `tests/test_pet_seam_particles.py` 完備。
      - **第4弾: `web_pet/pet_motion.js` 切り出し (✅ 2026-09-14 完了)**:
        - キャラクター管理・プリロード、生活リズム・睡眠サイクル、自律歩行物理（地面ランダム歩行・吹き出し追従）、なでなでインタラクション、大歓喜ジャンプ演出を完全独立Seam化。
      - **第5弾: `web_pet/pet_ui.js` 切り出し (✅ 2026-09-14 完了)**:
        - 高視認性HUDトースト、Glass Bottom Sheet（サジェスト／ニュースリーダー）、エージェント遠隔承認バナー（スワイプ／ノールックMediaSession連携）、時計・ポモドーロUI、エージェント稼働ライブバッジ、XSS防御（escapeHtml）を完全独立Seam化。`_hideBanner` 統合等の品質レビューAPPROVED獲得。単体テスト `tests/test_pet_seam_motion_ui.py` 完備。
      - **期待される効果 ＆ 達成実態**: 「UIや演出を直す時に通信や承認ロジックを壊す」玉突き事故が構造的に0%になり、AIの修正成功率とボスの5分コードレビュー可能性が最大化。`pet.js` の行数は当初3,335行から2,519行、さらに **1,604行**（通算約1,731行スリム化、半分以下に圧縮）へ大幅削減を達成。
   2. **バージョン定数の一元化 (`version.js` 動的配信 - ✅ 2026-09-14 完了)**:
      - `version.py` をSSOTとし、`local_sync_server.py` の動的配信および `sw.js`, `pet.js`, `index.html` の一元結合完了。Shotgun Surgery（散弾銃手術）を完全根絶。TDDテスト `tests/test_version_endpoint.py` 完備。
    3. **`database.py` (2,498行) の Repository パターン分割 (P2 / v1.2.0 - 🟡 Phase 1 完了 / Phase 2 準備完了)**:
       - **Phase 1 (✅ 2026-09-14 完了)**:
         - `storage/models.py`: Pydantic V2 モデル群の一元定義（13モデル）を先行分離。
         - `storage/connection.py`: コネクションライフサイクル・WAL設定・`init_db`・自動マイグレーションを集約。
         - `database.py`: 薄いFacadeとして再エクスポート維持。行数を2,498行 ➔ **1,960行** へスリム化。
         - 回帰防止テスト `tests/test_storage_seam_phase1.py` 完備、全体テスト446件全緑（444 passed, 2 skipped, 0 failed）達成。
       - **Phase 2 (次回着手予定)**:
         - `storage/audit_repo.py`: 改ざん耐性 承認監査ログ台帳（最優先）
         - `storage/device_repo.py`: ゼロトラスト端末台帳・SHA-256トークン照合・失効（最優先）
         - `storage/insight_repo.py`: 知識の宝庫 長期知見記憶・タグ検索・重要度抽出
         - `storage/note_repo.py`: 付箋メモ・デスクトップ座標・サイズ管理
         - `storage/habit_repo.py`: 習慣トラッカー・ストリーク計算・年間ヒートマップ
         - `storage/calendar_repo.py`: 予定・カテゴリ・複数カレンダー購読ソース・繰り返し展開
         - `storage/task_repo.py`: TODOタスク・TickTick風階層リスト・4象限マトリクス
         - `storage/minigame_repo.py`: ミニゲームハイスコア集計
         - `database.py` は全公開シンボルを再エクスポートする **Facade（Deep Module）** として維持し、71件以上の既存テストおよび全サブシステムの後方互換性を100%死守。ゼロオーバーヘッド（実行速度低下なし）。

### 13.8 🌐 グローバル展開: Web Showcase ＆ 公式チートシート英語完全版配備 (✅ 2026-09-13 達成)
- **状態**: ✅ 完了（2026-09-13）
- **成果**:
  1. **`README_en.md` ＆ `README.md` ビジュアル強化**: メインバナー画像（日本語/英語）、Mermaidダイアグラム、ASCIIデータフロー図、Showcase/チートシート公式バッジを配備。
  2. **公式チートシート英語版新設 (`docs/guides/NEO_HISHO_CHEAT_SHEETS_en.html`)**: 全5大セクション、日本語版との双方向トグル、および全6枚の公式インフォグラフィック英語版（超高精細・画面内UIまで完全英語化）を配備。
  3. **5大チートシートMarkdown英語版新設 (`docs/guides/cheatsheet_0X_..._en.md`)**: GitHub上で直接閲覧する海外エンジニアのため、①〜⑤の全Markdown版を英訳・インフォグラフィック埋め込み配備。
  4. **動く全体鳥瞰図 Web Showcase 英語版新設 (`docs/neo-secretary-showcase-en.html`)**: `eli5-data` / `eli5-ui` JSONの英訳、Archify設計図（`neo-secretary-architecture-en.html`）新設・結合により、完全無欠の英語Showcaseを配備。
  5. **GitHub Pages 404防止**: `.gitignore` にイグネア除外として `!docs/neo-secretary-showcase-en.html`, `!docs/neo-secretary-architecture-en.html` を追加。

### 13.9 🚀 広報・拡散: Qiita記事執筆完了 ＆ 3Dアイソメトリック対比図・5大インフォグラフィック配備 (✅ 2026-09-13 達成)
- **状態**: ✅ 完了（2026-09-13）
- **成果**:
  1. **Qiita記事本文ドラフト完成 (`docs/articles/qiita_draft_20260912.md`)**: NWエンジニア10年のリアルな課題感、古いスマホ転生のドラマ、自作MCPサーバー＆ゼロトラスト＆3段階スマート承認、引き算の美学を熱量高く完全執筆。
  2. **3Dアイソメトリック対比インフォグラフィック新規制作 (`docs/guides/assets/qiita_scene_comparison.jpg`)**: 「【Before】承認待ちで画面に釘付け」vs「【After】卓上スマホで自由＆即承認」を、既存インフォグラフィックと完全にテイスト統一した高品質3Dアイソメトリック図解として制作・配備。
  3. **5大インフォグラフィック配備**: メインバナー、古いスマホ転生（`cs02_mobile.jpg`）、4ステップMCP承認（`cs04_agent.jpg`）、ローカルLLM＆プライバシー（`cs05_localllm.jpg`）をストーリーの要所に埋め込み。
  4. **Qiita CDNキャッシュ対策**: URLリネームにより、Qiitaのimgixキャッシュによる画像未更新を恒久回避。
  5. **タグ・タイトル選定完了**: `Python`, `個人開発`, `AI`, `MCP`, `初心者` の5大タグおよび読者層に合わせたタイトル案を策定。

### 13.10 🏛️ 外部AI（GitHub Copilot）レビュー合意とガバナンス特化 (✅ 2026-09-14 策定)
- **状態**: ✅ 完了（2026-09-14）
- **背景と合意**:
  - GitHub Copilot によるブラインドレビュー対話録（`hisho-kun_review_conversation_export.md`）を精査。
  - **核心的洞察**: 世界のAI開発者・企業が真に求めるのは「かわいいペット」ではなく、**「AI Coding Agent Governance & Remote Approval Hub（離席中の代理承認基盤）」** である。
  - NW×PM×AIの視点を最大限に活かし、「壊れない運用設計」「人間参加型ガバナンス（Human-in-the-Loop）」をプロダクトの本命Moatとして確立する。
  - バージョン定数一元化（`version.js`）により散弾銃手術を根絶し、v1.1.0 Seam分割（`pet.js`, `database.py`）へスムーズに接続する。

### 13.11 ⚡ パフォーマンス最適化・省電力スプリント (✅ 完了・v1.0.3)
- **状態**: ✅ 全項目完了（2026-09-16 Cline Desktop が P1-2 / P1-3 / P1-4 を完遂し、省電力スプリント完結）
- **項目**:
  1. **【P0】`/api/status` 習慣＆ヒートマップのTTLキャッシュ化 (✅ 2026-09-15 Jules PR #4完了)** (`local_sync_server.py`):
     - 2秒ごとのポーリングで毎回直撃していた70日分ヒートマップ集計クエリを30秒TTLキャッシュへ移行（DB負荷93%削減）。追加・トグル・削除時の即時キャッシュ無効化（`invalidate_habit_cache`）を完備。単体テスト `tests/test_habit_cache.py` 完備。
  2. **【P0】スマホPWA 画面非表示時（`document.hidden`）ポーリング抑制 (✅ 2026-09-15 完了)** (`web_pet/pet.js`):
     - スマホ画面OFF/バックグラウンド時はポーリング間隔を 2秒 ➔ 30秒 (`FETCH_HIDDEN_INTERVAL = 30000`) に伸長し、バッテリー消費と発熱を根治。
     - 画面復帰時（`!document.hidden`）は待機タイマーを即座に破棄し、0秒即時フェッチ＆2秒通常ポーリングへ復帰（遅延ゼロ）。`pollingTimerId` と `clearTimeout` によるタイマー多重発火を完全防止。単体テスト `tests/test_pet_seam_pwa_power_save.py` 完備。
  3. **【P1-1】Canvas適応型描画ループ ＆ Wake-on-Demand (✅ 2026-09-16 完了)** (`web_pet/pet_particles.js`, `web_pet/pet.js`):
     - 画面非表示・スリープ時に `stopParticleLoop()` を自動発火し、`cancelAnimationFrame` で保留フレームを完全消去してループを即時停止（**0fps完全スリープ**、GPU/CPU負荷ゼロ）。
     - 画面復帰時やインタラクション（タップ・タスク完了セレブレーション紙吹雪）時のみ `wakeParticleLoop()` で最小限叩き起こす Wake-on-Demand 機構を配備。
     - `isParticleLoopRunning` フラグによる多重ループ防止、`web_pet/pet.js` 起動エントリーポイント統一、SSR/Node環境用 `typeof window !== 'undefined'` ガード完備。単体テスト `tests/test_pet_seam_canvas_power_save.py`（全8項目）完備。全体テスト474件全緑。
  4. **【P1-2】SQLite 頻出カラムの明示的インデックス追加 (✅ 2026-09-16 Cline完了)** (`storage/connection.py`):
     - `init_db()` に `idx_tasks_status_due (status, due_date)` / `idx_events_start_end (start_time, end_time)` を新設。テーブルフルスキャン (O(N)) をインデックススキャン (O(log N)) へ最適化し、`/api/status` の2秒ポーリングと手帳表示の負荷を低減。既存DBにも冪等に自動付与。単体テスト `tests/test_db_indexes.py`（6項目・EXPLAIN QUERY PLAN 検証）完備。
  5. **【P1-3】PC側メインループのアイドル待機30Hz化 (✅ 2026-09-16 Cline完了)** (`main.py`):
     - アイドル時の待機を 10ms (100Hz) から `MAIN_LOOP_IDLE_SLEEP_SEC = 0.03` (約30Hz) へ**固定レートで緩和**し、体感応答性（遅延上限=1ティック処理時間+30ms）を維持しつつ常時CPU占有を削減。単体テスト `tests/test_main_loop_power.py` 完備。
  6. **【P1-4】サーバー待受ポートの単一情報源 ＆ 環境変数オーバーライド (✅ 2026-09-16 Cline完了)** (`sync_config.py`):
     - `SERVER_PORT`（既定 8765 / `NEO_HISHO_PORT` で上書き可・不正値は既定へ安全退避）を唯一の情報源とし、`local_sync_server` / `ui/qr_dialog` / `main._auto_tailscale_serve` が参照。QRダイアログの URL ベタ書きを排除。単体テスト `tests/test_port_single_source.py` 完備。

### 13.12 🖼️ 秘書くんアイコン化 ＆ PWAホーム画面アイコン ＆ 端末管理UI (✅ 2026-09-16 完了)
- **状態**: ✅ 完了（Cline Desktop 実装・TDD 全項目グリーン）
- **項目**:
  1. **【タスク0】秘書くんアイコン化 ＆ キャラ着せ替え連動 (✅ 完了)**:
     - 新設 `ui/window_icon.py`（例外安全な `apply_window_icon` Seam）を main ウィンドウ・手帳・設定・QRダイアログへ適用。
     - トレイアイコンを `assets/dot/hisho/idle_1.png` へ変更し、`update_character_icon()` で着せ替え（hisho ⇄ kyle …）に連動。キャラIDはパストラバーサル検証済み。
     - `neo_hisho.spec` の `icon=None` を `icon=str(ICON_PATH)`（assets/icon.ico・256x256）へ変更。単体テスト `tests/test_tray_character_icon.py` / `tests/test_window_icon_seam.py` 完備。
  2. **【タスク1】ハードコード解消 (✅ 完了)**:
     - `ui/qr_dialog.py` の `http://localhost:8765/api/test_buzz` を `build_loopback_api_url()` へ置換し、ポートは `sync_config.SERVER_PORT` から解決。`*.bak` 系除外は維持。
  3. **【タスク2】省電力完遂 (✅ 完了)**: P1-2 / P1-3（上記 13.11）。
  4. **【タスク3】PWAホーム画面アイコン最適化 (✅ 完了)**:
     - `assets/pwa/` に 192/512（any）・512（maskable）・180（apple-touch-icon）を生成（`tools/build_pwa_icons.py`）。
     - `web_pet/manifest.json` の 404 参照とサイズ不一致を解消、`web_pet/index.html` に apple-touch-icon / favicon / app-title を追加、`web_pet/sw.js` に4アイコンを事前キャッシュ。単体テスト `tests/test_pwa_icons.py` 完備。
  5. **【タスク4】設定画面ゼロトラスト端末一覧UI (✅ 完了・Sprint C 先取り)**:
     - 新設 `ui/device_manager_panel.py`（Seam `build_device_rows` / `list_device_rows` / `revoke_device_entry` ＋ `DeviceManagerSection`）を設定画面の「📱 接続端末管理」タブへ搭載。確認ダイアログ → Revoke → 即時再描画。単体テスト `tests/test_device_ui_seam.py` 完備。

### 13.13 🧠 用語境界規約の恒久化：MentisDB ≠ 知識の宝庫 (✅ 2026-09-16 完了)
- **状態**: ✅ 完了（2026-09-16 Cline Desktop セッションで発生した「MentisDB ＝ 知識の宝庫」という同一視ミスの根治）
- **定義**:
  1. **知識の宝庫 (Knowledge Vault)** ＝ 秘書くんアプリ内のボス知見ストア（`storage/insight_repo.py`・`user_insights` テーブル／`neo_secretary.db`）。書き込み `remember_boss_insight` ／ 読み出し `get_boss_insights`（neo_hisho_bridge）。
  2. **MentisDB** ＝ Cline環境側の汎用エージェント記憶MCP。**別物・混同禁止**。
  3. **codebase-memory-mcp** ＝ コード知識グラフ ＋ ADR（`manage_adr`）。設計決定・Gotchaはこちらへ。
- **恒久対策**:
  1. **規約化**: `AGENTS.md` §2.10「用語・記憶装置の境界規約」（書き分けの原則・2026-09-14 呼称刷新の歴史を明記）。
  2. **仕様書への注記**: `DESIGN_SPEC.md` §5.2 ／ `MCP_INTEGRATION.md`（remember_boss_insight の節）に境界注記を追記。
  3. **混同記述の一掃**: `active_context.md`（4箇所）／ `docs/learning-memos/学習メモ_20260916.md` ／ `学習メモ_20260912.md`（旧呼称注記）／ `.agents/skills/`（deep-audit・graph-engineering・holistic-code-review）／ `docs/developer-ecosystem-*.html`（3種4箇所）を修正。
  4. **番人テスト（Canary）**: `tests/test_terminology_boundary.py` 新設。エージェントが最初に読む `active_context.md`・`.agents/skills/*/SKILL.md`・`AGENTS.md` へ混同が再流入した瞬間に Red で検知する。
- **歴史記録の扱い**: 2026-09-14 呼称刷新以前のドキュメント（学習メモ 2026-08-15〜09-13、handover archive、docs/temp の計画書・レビュー報告、`developer-ecosystem.architecture.json` の内部ID `mentis_db`）に残る "MentisDB" は **当時の呼称の歴史記録** として現状維持とする。

### 13.14 🔒 セキュリティP0修正 ＆ テスト健全化 (✅ 2026-09-16 完了)
- **状態**: ✅ 完了（ruthless-code-evaluation の独立査読2名＋統括で検出 → 即時修正）
- **P0-1**: `GET /assets/...` の未認証任意ファイル読み出し（Windows `\` 絶対パスで assets 外へ脱出、`0.0.0.0` 待受）を根治。
  新規 `web_assets.py`（純粋 Seam `resolve_asset_path`）へ検証を集約し、①危険表現排除 ②画像アローリスト ③ルート配下検証の多層防御。
  PoC実測: 攻撃系すべて 404・漏えいなし / 正規アイコンは 200。テスト `tests/test_asset_path_security.py`（14件）。
- **P0-2**: `tests/test_port_single_source.py` の `importlib.reload`＋`os.environ.pop` を全廃し、環境変数検証をサブプロセス隔離へ。
  `NEO_HISHO_PORT=1500` 環境で全586件 green（修正前は同条件で5件FAILの偽陽性）。
- **P1-1**: `.env` の `NEO_HISHO_PORT` も有効化（`read_port_from_env_file`。解決順: OS環境変数 → `.env` → 既定8765）。`.env.example` の案内が実装どおりに。
- **P1-2**: ✅ **完了（2026-09-16 追補）** — `agent_bridge_client`（`resolve_hub_port` 新設）／`ui/settings_window` 3箇所／`character_manager`／`web_pet/pet.js`／`DESIGN_SPEC`／`.agents/*` を `sync_config.SERVER_PORT` 参照へ統一。出荷コード全体の語境界走査テストを新設。
- **P1-2'（独立査読で発見）**: ✅ 完了 — 出荷ドキュメント（`docs/guides/*`・`README.md`・`.env.example`）に「既定8765／`NEO_HISHO_PORT` で変更可」を併記し、`TestShippedDocsPortGuidance` で恒久化。
- **P1-2''（独立査読で発見）**: ✅ 完了 — 同梱列挙を `build_resources.py` へ Deep Module 化（`*.bak` 等を除外）。`docs/guides/NEO_HISHO_CHEAT_SHEETS.html.bak` の削除はボス判断待ち（`git rm` コマンドを提示済み）。
- **P1-3 / P1-4**: ✅ **完了（2026-09-16）** — ①端末パネルの台帳読み出しを**ワーカースレッド＋`post_action` 経由のメインスレッド反映**へ（UIブロック根絶・多重発行抑止・同一内容の再描画スキップ・失敗の明示表示）②Revoke を **UI/API 両経路で監査ログ記録**（`record_device_revoke`・同期書き込み）＋API処理を `api_devices.handle_post_devices_revoke` へ Seam 化。
- **P1-3' / P1-4'（独立査読で発見）**: ✅ 完了 — 失効直後の再読込が破棄されて**UIが嘘をつく**不具合を「延期＋楽観反映」で根治（回帰テスト付き）、監査の非同期消失を**同期書き込み＋終了時フラッシュ**で根治。
- **残負債**: P1-6（DB索引テストの実クエリ化）／P2群（SW `cache.addAll` 耐性・アセット解決一本化・maskable余白・起動時インデックス非同期化・Tailscale失敗UX）／P3群／**端末台帳は実質1行**（グローバルトークン1本・`token_hash UNIQUE`）＝個別失効UIの期待値ギャップ／un-revoke API不在。詳細は `docs/code_review_report_20260916_ruthless.md`。

### 13.15 🧠 LLM出力言語ガード ＆ 品質負債の全返済 (✅ 2026-09-17 完了)
- **状態**: ✅ 完了（P0含む全項目。実機確認済み）
- **P0: 生活コーチの中国語化修正**:
  - 原因: LLMプロンプトが `Output language: ja` と**言語コードのみ**を渡しており、DeepSeek系モデルが既定言語（中国語）で出力していた。
  - 対策: `i18n.LANGUAGE_NAMES`／`get_language_name()`／`get_no_chinese_instruction()` を新設し**正式言語名＋中国語禁止文**を全LLMプロンプトへ適用。`agent.py` のシステムプロンプトに【言語規則】を明記。`life_coach_engine` はレポートに `language` を埋め、**言語変更時は旧レポートを破棄して再生成**（SQLite永続化を跨ぐ残存防止）。詳細は `DESIGN_SPEC §20.9`。
- **P1-6**: DB索引テストの捏造SQLを廃止し `calendar_repo.py` の実クエリ構造へ準拠。
- **P2群**: ①`sw.js` の `cache.addAll` → 個別 `cache.add`（1件404でも全滅しない）②**アセット解決の一本化**（`iter_tray_icon_candidates`）＋**起動時トレイアイコンが着せ替え状態に追従** ③**Tailscaleコマンドの `--bg` 統一**（`sync_config` を単一情報源化）④maskable余白 0.15→0.08 ⑤`init_db` 実行時間の可視化（2秒超で警告）⑥端末管理UIの復旧手順を正確化＋台帳1件の注記。
- **P3群**: ①「適応型スリープ」→「アイドル待機の30Hz化（固定レート）」へ命名是正（遅延上限=1ティック処理時間+30ms）②`tools/build_pwa_icons.py --check`（ドリフト検出）＋テストで凍結 ③`scan_git_secrets.py` の cp932 クラッシュ修正 ④タブコメント番号／`.env.example` 末尾改行。
- **テスト**: `631 passed / 3 skipped / 0 failed`（skipped は多重起動ロック2件＋Tk環境依存1件）。
- **残課題（設計）**: 端末台帳の**個別トークン化**（現状は全端末共通トークン＋`token_hash UNIQUE` のため台帳は実質1行）／**un-revoke API** 不在。→ 13.16 にて完了。

### 13.16 🛡️ 端末台帳のゼロトラスト個別トークン化 ＆ 端末単位 un-revoke API (✅ 2026-09-18 完了)
- **状態**: ✅ 完了（TDD Red→Green、`quality-reviewer` 多角査読 APPROVED 獲得）
- **背景**:
  従来は全端末共通の単一グローバルトークン（`.sync_token`）を共有していたため、`devices` テーブルが実質1行のみとなり、「複数端末の個別管理・個別失効」ができない構造的ギャップを抱えていた。
- **実装内容**:
  1. **QRペアリング単位での端末固有トークン発行 (`storage/device_repo.py`)**:
     - `issue_device_token()`: 256bit 暗号論的乱数 (`secrets.token_hex(32)`) を発行し、DBにはSHA-256ハッシュのみを永続化（平文トークンはメモリ返却のみのゼロトラスト徹底）。
     - `/api/auth/token`: ペアリング開放時および外部端末からの要求に対して個別トークンを新規発行・返却。同一PC内ローカルアクセスは後方互換性のためグローバルトークンを維持。
  2. **2層 Bearer 認証 (`local_sync_server._check_auth`)**:
     - グローバルトークン（マスターキー）または個別端末トークンの2層判定。
     - 端末Aを失効させても端末Bは影響を受けずに通信継続（真の端末個別失効を達成）。
     - `dev.token_hash` を直接 `touch_device_last_seen` へ渡し、Deep Module原則を遵守。
  3. **端末単位の復帰（un-revoke）API ＆ UI (`api_devices.py`, `ui/device_manager_panel.py`)**:
     - `POST /api/devices/restore`: ループバック限定・二重防御ハンドラ新設（`api_devices.handle_post_devices_restore`）。
     - `storage.device_repo.restore_device(device_id)`: `UPDATE devices SET is_revoked = 0 WHERE id = ?`
     - `record_device_restore()`: 復帰操作を `approval_audit_logs` へ同期記録（who/when/which device）。
     - 設定画面UI: 失効済み端末カードに「♻️ 接続復帰」ボタンを配備。確認ダイアログ・ヘッダー説明文を最新化。
  4. **テスト・品質ゲート**:
     - `tests/test_device_individual_tokens.py`（新規TDDテスト）、`tests/test_api_devices_seam.py`、`tests/test_device_ui_seam.py`。
     - 全テスト 636 passed / 3 skipped。独立検証者（`quality-reviewer`）多角査読 APPROVED。

### 13.17 📱 未承認端末接続時の Human-in-the-Loop 承認ダイアログ (P0-2) ＆ Jev MCP インフラ完全開通 (✅ 2026-09-20 完了)
- **状態**: ✅ 完了（TDD Red→Green 7件合格、OpenCode GO 実機 kimi-k2.7-code 査読完了、Jev MCP 4 tools オンライン化）
- **背景**:
  ゼロトラスト原則に基づき、スマホ等の外部端末からトークン配布要求（`/api/auth/token`）が届いた際、ペアリング開放中であってもデスクトップPC上で人間の明示的な許可（Human-in-the-Loop）を求める防壁を構築した。
- **実装内容**:
  1. **承認ダイアログ GUI (`ui/device_approval_dialog.py`)**:
     - `DeviceApprovalDialog`: 最前面表示（`-topmost`）、接続元端末名・IP・安全審査バッジ・30秒カウントダウン表示。
     - 自動拒絶（Fail-Closed）: 30秒タイムアウト、ダイアログ閉じるボタン、例外発生時はすべて自動拒絶。
     - スレッド同期 Seam: `ask_device_approval_gui` により、HTTPワーカースレッドからメインGUIスレッドへ安全に委任・待機（`threading.Event`）。
  2. **同期サーバー認証ハンドラ結合 (`local_sync_server.py`)**:
     - `set_gui_instance`: GUIインスタンス登録時に `SyncTokenManager.set_device_approval_callback` を自動登録。
     - `DeskPetSyncHandler._handle_auth_token`: ループバック（`127.0.0.1` / `::1`）は即時許可、外部IPからの接続要求時は人間承認コールバックを呼び出し、拒絶時は 403 Forbidden 返却。
  3. **OpenCode GO × TypeSafe Jev 実機査読・MCP開通**:
     - `jev-mcp` を独立仮想環境（`~/.gemini/tools/jev_router/.venv`）にて公式MCPサーバーとしてAntigravityに統合（4 tools enabled）。
     - OpenCode GO実在モデル一覧からJevが `kimi-k2.7-code` を動的選定し、実機コード査読（4000トークン思考）を完了・証拠永続化（`docs/opencode_review_result.json`）。
  4. **テスト**:
     - `tests/test_device_connection_approval.py`（7件全件合格）。拒否・許可・Fail-Closed・ループバックバイパス・コールバック例外の網羅的検証。

### 13.18 🤖 エージェント Hooks 設定ガイドウィザード ＆ 新・機能オンボーディングツアー（日英完全対応） (🔲 次期スプリント計画)
- **状態**: 🔲 次期スプリント優先項目 (v1.5.0)
- **背景と課題 (Why)**:
  1. **エージェント側の導入摩擦解消**: Antigravity の `PreToolUse` Hook や OpenCode の `hisho-approval-notify` プラグイン等、エージェント側へ設定を配備することで「承認待ちで離席中に作業が止まる痛点」を完全解決できる。しかし一般ユーザーにとって、各エージェントの設定ファイルを探して JSON や TypeScript を配置するのはハードルが高い。秘書くん単体でこの機能を強くアピールし、1分でセットアップを完了できる「ガイド／自動配備ウィザード」が不可欠である。
  2. **ツアー機能の陳腐化解消**: 初期のオンボーディングツアー（`tour_engine.py` / `ui/tour_overlay.py`）は2026-09-01時点の3ステップのままであり、その後に実装された最大のキラー機能（Agent Bridge、Desk Pet遠隔承認、適応型Canvas 0fps省電力、統合手帳、多言語同期）が案内されていない。
  3. **グローバル対応（英語版ツアーの最新化）**: グローバルエンジニアへ向け、英語環境でも最新機能が魅力的に伝わる英語オンボーディングツアーを完備する必要がある。
- **アーキテクチャ対応方針 (What)**:
  1. **エージェント Hooks 設定ガイドウィザード (`ui/hooks_guide_dialog.py` / 設定画面連携)**:
     - 各主要エージェント（Antigravity, OpenCode, Claude Code, Cursor, Codex 等）の Hooks / プラグイン設定スニペットのワンクリックコピー機能。
     - 可能であれば、ユーザーのローカル設定フォルダ（`~/.gemini/config/hooks.json` や `~/.config/opencode/` 等）を自動検知し、ワンタップで設定を追記・配備するアシスタント機能。
     - 「🔔 通知テスト（Ping）」ボタン: エージェント設定が正しく完了したかを、その場でテストリクエストを投げて PCペットの大暴れとスマホDesk PetのBuzz振動で確認できるセルフ診断機能。
  2. **新・機能オンボーディングツアーの刷新 (`tour_engine.py`, `ui/tour_overlay.py`)**:
     - 秘書くんの現在の真のコア価値を伝える4ステップ構成へ刷新：
       - Step 1: **🎉 ようこそネオ秘書くんへ**（右クリックメニュー・常駐ペット・基本操作）
       - Step 2: **📱 スマホとつなぐ卓上リモコン**（QRワンタップペアリング・Desk Pet 0fps省電力・愛着）
       - Step 3: **🤖 コーディングエージェント遠隔承認**（Agent Bridge・Hooks連携・離席中のノールック承認）
       - Step 4: **📔 統合手帳・カレンダー ＆ 言語設定**（TODO・習慣・iCal連携・日英切替）
  3. **日英完全対応の多言語ツアー辞書 (`i18n.py`)**:
     - `tour.settings_menu`, `tour.mobile_qr`, `tour.agent_bridge`, `tour.notebook_i18n` の日英テキストを整備。
     - 英語表記においても「文字数予算（Microcopy規約）」を厳格に守り、吹き出しの視認性とレトロな世界観を両立。
     - 右クリックメニュー「🎓 使い方ツアー」からいつでも再受講可能とする。

### 13.19 🛠️ 技術的負債・品質是正バックログ（手帳TODO同期アイテム） (🔲 スプリント計画候補)
- **状態**: 🔲 バックログ待機（手帳同期）
- **背景**: 過去のコードレビューやセッション中にAIエージェントが自律的に秘書くん手帳（TODO）に起票した宿題・課題を、ロードマップの公式トラッキングへ正式編入したもの。
- **課題一覧**:
  1. **【セキュリティ宿題】`~/.gemini/config/mcp_config.json` の平文シークレット是正**:
     - 概要: Antigravity側グローバル設定内の平文APIキー2件を `{env:VAR_NAME}` 参照へ置換し、設定ファイルからの漏洩リスクを根絶する。
     - 優先度: 高
  2. **【P1】`approval_audit_logs` テーブルのスキーマ・語彙契約適合**:
     - 概要: `created_at` の日時精度（ミリ秒 vs 秒）の整合性確認、および `decision`, `decision_by` カラムの値が設計正本（DESIGN_SPEC §10）の語彙一覧と厳密に一致しているかのテスト整備と是正。
     - 優先度: 高
  3. **【P1】`events.google_event_id` の重複防止の完全性確認**:
     - 概要: iCal同期（`ics_tools.py`）で `DELETE FROM events WHERE source_id = ?` の一括置換が導入済みであるが、手動追加や古いデータにおける同一 `google_event_id` の重複レコードのクリーンアップマイグレーションの実施。
     - 優先度: 高
  4. **【P1】スマホ通知の Web Push API 導入（コアバリューの完成）**:
     - 概要: 現行のスマホ通知は **PWA が開いている間の 2秒ポーリング方式のみ**で、画面オフ・ブラウザ閉じ・スリープ中は**原理的に届かない**。2026-09-22 の実測で確定（通知発火 `00:32:54Z / req_c61c2b8a` 時も `devices.last_seen` = **901.7分前＝未接続**）。つまり「席を外しても気づく」コアバリューが構造的に未成立。**Service Worker + VAPID** で Web Push を導入し、PWA を閉じていても OS 標準通知として届ける。加えて PWA 起動時の**取りこぼした通知のまとめ表示（未読バッジ）**を併設する。
     - エビデンス: Plugin → 秘書くん(PC) 経路は `HTTP 200 queued` で**正常**（問題は受信側ポーリングのみ）／`%TEMP%\hisho-notify.log` に全履歴あり
     - 優先度: 高（実装コスト: 中）／手帳 TODO **ID 30** と同時起票済み（AGENTS.md §2.13）／**2026-09-22 サーバ・PWA 両側実装完了（詳細は DESIGN_SPEC §24）・実機 E2E 待ち**:
        - サーバ側: `storage/push_subscription_repo.py`（購読台帳）／`storage/vapid_key_repo.py`（VAPID 鍵生成・永続化）／`api_push.py`（`POST /api/push/subscribe|unsubscribe`・`GET /api/push/vapid_key`）／`push_sender.py`（pywebpush fire-and-forget・404/410 無効購読掃除）／通知フック3箇所（ask 承認待ち・ask_input 質問・notify 完了）＋リマインダー `_dispatch` 配線
        - PWA 側: `web_pet/sw.js`（push 受信→OS 通知＋IndexedDB 未読記録・notificationclick→アプリ復帰）／`web_pet/pet_push.js`（SW 登録・購読・**起動時取りこぼしまとめバナー**）／`web_pet/push_common.js`（SW・ページ共用 IDB ユーティリティ）
        - テスト: `tests/test_web_push_server.py` **24件 ALL GREEN**／全回帰 **785 passed / 2 skipped**
        - 運用メモ: 秘書くんアプリの**再起動が必要**（Python 側の新 API は起動時ロードのため）／VAPID 鍵は DB に永続化し再生成しない（再生成すると全端末の購読が無効化される）
        - **✅ 実機 E2E 完了 (2026-09-22 16:58)**: HTTPS (Tailscale) 接続で購読登録 → 画面OFF の状態で `effect:ask` 発火 → **OS 標準通知を受信**（FCM 201 受理確認済み）→ PWA 起動時の「🔔 オフライン中の通知 N 件」まとめバナーも確認。**コアバリュー「席を外しても気づく」が構造的に成立**。 Gotcha: ① LAN (http://192.168.x.x) はセキュアコンテキスト例外で SW/Push が使えない（Tailscale HTTPS が必須・QR既定）② 同一コマンドの「常に許可」で effect:allow になると通知は発火しない（設計どおり）③ テスト用 ask コマンドは allow 化済みのものを使わない
   5. **【P2】llm_factory 起動時モデル巡回の設定済みプロバイダ限定**:
      - 概要: 起動時に `LLMProvider` 全10プロバイダを無条件巡回（`main.py` → `llm_factory.sync_all_discovered_models`）し、APIキー未設定（openai/groq/openrouter）・ローカルサーバー未起動（ollama/lm_studio/custom_openai）の6プロバイダで**確定失敗のネットワーク試行＋ERRORログ＋フォールバックロード**が毎回発生（実測約4.6秒）。修正方針: ① クラウド系は `is_provider_configured()==False` ならスキップ（ERROR→INFO降格）② ローカルサーバー系は TCP 接続プローブ（0.5秒タイムアウト）成功時のみ取得 ③ 現行選択中プロバイダは常に巡回。llm_factory.py 約30行＋テスト。
      - 優先度: 中 ／ 手帳 TODO **ID 31** と同時起票（AGENTS.md §2.13）
      - **✅ 完了 (2026-09-22)**: `should_probe_provider` / `_probe_endpoint` 実装・`tests/test_llm_factory_sync_gating.py` 8件（Red→Green）・独立検証者（agent-tester `ses_f37d4c53bffe7PSGbsxEuFqXK3`）**PASSED**。実測で未設定7プロバイダの「⏭ スキップ」＆ERROR 0件を確認。詳細は DESIGN_SPEC §25。
   6. **【P2】gemini モデル一覧取得の恒常 400 Bad Request 是正**:
      - 概要: gemini は設定済みだが取得が恒常失敗。`google.generativeai` SDK 未導入で REST フォールバックへ転落し HTTP 400。フォールバック定義5件で運用中。SDK 導入可否検討（`langchain-google-genai` と競合確認）または REST パラメータ再検証。
      - 優先度: 中 ／ 手帳 TODO **ID 33** と同時起票
   7. **【P3】モデル同期スキップ理由のラベル分割 (APIキー未設定 / サーバー未起動)**:
      - 概要: `llm_factory.SKIP_REASON` が一括のため障害調査時の切り分けが困難。理由別ラベル化とテスト断言の更新。
      - 優先度: 低 ／ 手帳 TODO **ID 34** と同時起票
   8. **【P1】引き算スプリント第1弾: Whisper音声認識の完全撤去（2026-09-22 ボス決定）**:
      - 背景: 総合コードレビュー（ruthless-code-evaluation）の「引き算」判定（D評価）とボス判断によるスコープ縮小。ボス実測で「音声認識精度が低すぎた」ため、内蔵Whisperを維持する意義が消滅。**スマホOS標準の音声入力（キーボードのマイク）で同等以上の体験が代替可能**。
      - 対象フットプリント: `whisper_transcriber.py`（199行・faster-whisper は optional import / requirements.txt 未記載＝配布版では元々無効）／`api_tasks.action_transcribe_voice`（~99行）／`web_pet/pet.js` の `startVoiceInput`＋`_sendVoiceToWhisper`（~153行）／`tools/e2e_voice_check.py`（96行）／`tests/test_whisper_transcriber.py`／CHEAT_SHEETS 記載。
      - 実施手順: 対象テスト同時削除 → 全回帰 Green → PWAマイクUI撤去 → ドキュメント4点セット同期（Git履歴で復元可能）。
      - 優先度: 高（引き算第1弾・独立低リスク）／手帳 TODO **ID 35** と同時起票（AGENTS.md §2.13）／**実施タイミング: P0セキュリティ修正完了後**
   9. **【P1】引き算スプリント第2弾: LifeCoachEngine の撤去（2026-09-22 ボス決定）**:
      - 背景: 2時間ごとに LLM を呼び出して生活分析・サジェストを生成しているが、ボスの行動変容に寄与していない（＝トークン代を払って通知ノイズを生成）。レビューの「百貨店化」指摘に合致。
      - 対象フットプリント: `life_coach_engine.py`（**511行**・`_invoke_llm`・専用テーブル・スケジューラ連携・サジェスト更新）／`main.py` 配線（~249-252）／`local_sync_server.py` の status payload（`life_coach` フィールド ~1353-1393）／`tests/test_life_coach_engine.py`＋`tests/test_scheduler_periodic.py` 参照／i18n キー。
      - **重要**: `LifeDreamer`（天気・生活イベント＝ペットの情緒・コアバリュー）は**残す**。混同による誤削除を禁止する。
      - 優先度: 高（LLMコスト削減）／手帳 TODO **ID 36** と同時起票／**実施タイミング: P0セキュリティ修正完了後**
  10. **【P3】neo_hisho_bridge に update_task ツール追加（priority/memo の後編集）**:
      - 背景: 2026-09-22、AIが `create_task` の priority に誤った形式（"P1" 等の文字列）を渡し、全タスクが priority=0（無効値）で登録される事故が発生。正しい仕様は `high` / `medium` / `low`（`hisho_mcp_server.py:184-189` で high=3/medium=2/low=1 にマッピング、無効値は 0）。さらに **`update_task` が存在せず後から修正できない**ことが判明（今回はDB直接UPDATE 5行で対処）。
      - 方針: ① `update_task(task_id, priority?, memo?)` を追加 ② `create_task` の priority 引数に enum バリデーションを追加し無効値(0)の混入を構造的に防ぐ。
      - 優先度: 低 ／ 手帳 TODO **ID 37** と同時起票
  11. **【P1】引き算に伴う説明資料・配布仕様の一括更新（Whisper/LifeCoach 撤去と不可分・2026-09-22 ボス指示）**:
      - 背景: 撤去決定（第8/9項）に伴い、README・チートシート・Showcase・設計書・キャラ説明文・配布 spec を同期しないと**「できる機能」と実態が乖離した資料が残る**。ボス指示により単独タスク化（忘れ防止）。
      - 影響調査済み: `docs/guides/cheatsheet_05_local_llm_voice.md`（Whisper章削除）／`docs/guides/NEO_HISHO_CHEAT_SHEETS.html`＋`_en.html`（§2.5.1リリース同期義務）／`docs/neo-secretary-showcase.html`／`docs/neo-secretary-architecture.html:5441`／DESIGN_SPEC（凍結表L23・§9.5・生活コーチ節L839）／機能ロードマップ（11.6/11.8/B20/サイクル1/5.6）／`character_manager.py:56,102`（キャラ説明文）／`neo_hiso.spec`／`i18n.py`。**README.md は言及ゼロで修正不要（確認済み）**。
      - 制約: `docs/releases/*`・`docs/code_review_report_*`・`active_context.md` 等の履歴文書は編集禁止（追記のみ）。**✅ ボス決定 (2026-09-22): 音声入力（Web Speech＋Whisper）は全撤去。TTS（朝会ブリーフィング読み上げ `toggleBriefingSpeech`）は稼働中の別機能（依存ゼロ・コストゼロ）のため残す**。
      - 優先度: 高（35/36 と同一スプリントで実施＝不可分）／手帳 TODO **ID 38** と同時起票
  12. **【P0】総合コードレビュー確定 P0×4 の一括修正（2026-09-22 レビュー完了 → 最優先）**:
      - 背景: 4スキル×並列レビュー（holistic-code-review / ruthless-code-evaluation / codebase-design / deep-audit）が完走。レポート: `docs/reviews/2026-09-22_deep_audit.md`（68KB・13セクション・補遺含む）。
      - ✅ **P0-1（ID 39）完了 (2026-09-22 / v1.1.7)**: `/api/status` の sync_token 漏洩＋失効巻き戻しの根治。
        - **実装（3層）**: ①payload からマスターキー同梱を削除（`local_sync_server.py`）＋DTO 境界の deny-list（`sync_dtos.py`）②PWA のトークン上書き撤去（`web_pet/pet.js` → 更新経路は `pet_auth.js requestSyncToken` 一本化）③失効の自動巻き戻し廃止（`register_device` の `is_revoked=0` 削除／失効解除は `restore_device` のみ）
        - **A案（ゼロトラスト硬化・ボス承認）**: 認証経路の台帳書き込みを全廃（`sync_device_session` は読み取り専用化、資格情報の束縛は人間承認済みペアリング経路 `reuse_identity=True` のみ）＋非ループバックのマスタートークンは台帳照合必須（未登録・照合例外は Fail-Closed 401）→ **漏洩済みマスターキーは即無効**（旧スマホは1回の再ペアリングで復帰）
        - **人間承認済み再ペアリング = 明示復帰**: `restore_device` ＋監査ログ `source="pairing"`（client_ip 付き）
        - **検証**: 新規14件（`tests/test_p0_sync_token_leak_and_revocation.py`）＋契約更新（`test_device_bearer_seam.py` / `test_device_individual_tokens.py` / `test_storage_seam_phase2_step1.py` / `test_pet_seam_auth.py` / `test_sync_dtos.py`）。Red 8 failed → Green、全回帰 **806 passed / 4 skipped**。独立検証: `devils-advocate`（P0×1指摘→A案で封鎖）／`quality-reviewer`（CHANGES_REQUESTED→指摘を是正、残余は下記バックログへ）
        - **残余リスク（§13.19 へ起票）**: マスタートークン経路の Tailscale Serve 同一性（P0-3）／Host ヘッダー未検証（DNS Rebinding）／revoke×restore レース／ペアリング600秒窓と 403 自動再ペアリング／`.sync_token` 書込失敗フォールバック
      - **P0-2（ID 40）**: 承認ポリシーのバイパス（`approval_policy.py:155` が `;` `&&` `||` のみ検知）→ `shlex` 構造パース＋STRICT追加（TDD）。
        - ✅ **実装完了 (2026-09-22 23:00 / 全回帰 833 passed)**: `shlex`（posix + `punctuation_chars`）による構造判定へ移行。
          - パイプ・リダイレクト・`&`・チェイン・**コマンド置換/変数展開**（`$()`・バッククォート・`%VAR%`・`$env:`・`${}`）・改行 → 🟡 Prompt。
          - パイプ先インタプリタ / `xargs`＋インタプリタ / `find -exec|-delete|-fprint` / `eval|iex` / `git --output` / `git branch -D`・`tag -d`・`remote add|remove` / `rm --recursive --force` 系 → 🔴 Strict。
          - **shlex 既定値の罠2件を封鎖**（quality-reviewer 指摘 P0）: `commenters='#'` の語中読み捨て（`echo pwn#>~/.bashrc`）／句読点ランの連結トークン（`>|` `&>` `&>>`）。
          - **過剰 Strict の防止**（同 P1）: 冗長な `curl|wget` パイプ判定を削除し、危険語は非クォートのトークンで判定（`grep -r 'eval(' .` は Auto-Allow 維持）。
          - テスト: `tests/test_p0_2_approval_policy_structural.py`（19テスト＋40 subtests）／既存 `test_approval_policy.py` 回帰維持。
      - **P0-3（ID 41）**: Tailscale Serve 経由 client_ip 同一性崩壊（127.0.0.1 中継で loopback 信頼が全 tailnet 化）→ 実機1行検証→トークン種別ベース信頼設計へ（ADR更新必須）。
        - 🔴 **2026-09-22 第2ラウンド検証（devils-advocate `ses_f370ece1dffeCZXg3DrgYAVE99`）で具体的攻撃チェーンを実証**: Serve 経由の接続は `_is_loopback` が真になり ① `GET /api/auth/token` が**マスター鍵を無条件配布**（ペアリング・承認不要）② `POST /api/devices/restore` で失効端末が復活 ③ `POST /api/agent/ask`（loopback限定）が通り**承認RCEチェーンが再開放**。監査ログの actor も "localhost" に偽装される。
        - ✅ **実装完了 (2026-09-22 22:00 / 全回帰 816 passed)**: **loopback 信頼の3条件化**で根治。
          1. **マスター鍵配布の物理撤去**: `/api/auth/token` の loopback 分岐は `tm.loopback_token`（プロセス内生成・台帳非登録）を返し、**マスター鍵は HTTP で一切配布しない**。
          2. **`_is_trusted_loopback(client_ip, host_header, headers)`**: ①TCPピアが loopback ②プロキシヘッダ（X-Forwarded-For / Forwarded / X-Real-IP）が無い ③**Host が loopback 名**（Host 無しは Fail-Closed）。
          3. **loopback 専用トークンは非ループバックで 401**（窃取しても Serve/LAN から使えない）。適用箇所: `_check_auth` / `_handle_auth_token` / `_dispatch_get_devices` / トークン無し閲覧 / `_check_webhook_auth` / AGENT_ONLY / devices revoke・restore / agent activity。
          - **Serve の識別**: Serve は TCPピアを 127.0.0.1 に中継するが Host は `*.ts.net` のため条件③で「外部端末」に分類され、**ペアリング開放 + 人間承認 + 個別トークン（失効・監査の対象）**になる。
          - テスト: `tests/test_p0_3_loopback_trust.py`（6件＋3 subtests）／`test_device_connection_approval.py` に Serve 相当ケース追加。
        - 🔎 **残余（ID 47 へ集約）**: 明示的な Host **許可リスト（400 応答）** と `*.ts.net` 以外のプロキシ運用への対応。実機確認 `tailscale serve status` の1行検証は推奨事項として残る。
      - **P0-4（ID 42）**: ✅ **完了 (2026-09-23)**: VAPID 秘密鍵＋DB の OneDrive 退避（ADR-2: 暗号鍵保存境界）。
        - **実装**: `app_paths` にデータ境界 API（`get_data_root`/`get_db_path`/`get_sync_token_path`/`get_backups_dir`/`is_cloud_synced_path`）を新設し、既定は `%LOCALAPPDATA%\NeoHisho`（env `NEO_HISHO_DATA_DIR` で上書き可）。`storage.connection.resolve_db_path` を**単一チョークポイント**として 100超の呼び出し元を無改修で新境界へ到達（`sqlite3.connect` は production では同ファイルと移行エンジンのみ＝機械凍結テスト）。
        - **安全移行**: `migrate_legacy_data()` = Online Backup API（WAL込み一貫スナップショット）→ `quick_check` → 一意名 `*.migrating` 経由で本採用 → 旧ファイルは `migration_archive/` へ**退避（移動・削除しない）**。`-wal`/`-shm` は本体と運命共同体（オール・オア・ナッシング）。移行先既存時は上書きせず残存旧DBの再退避を試行し、失敗時は「機密残存」を ERROR で明示。クラウド同期フォルダへの移行は **Fail-Closed 拒否**。**移行の実行者はアプリ (`main.py`) ただ1つ**（`hisho_mcp_server.py` は警告のみ＝アプリ稼働中の移行は DB 分裂を生むため）。
        - **検証**: 新規 `tests/test_p0_4_data_boundary.py`（Red 実測 → Green）＋ `hisho_mcp_server` への起動時移行配線 ＋ `tests/conftest.py` によるテスト隔離（`NEO_HISHO_DATA_DIR` 強制上書き＋「データルート外解決で即失敗」ガード）。独立検証: quality-reviewer（mimo-v2.6-pro）2巡で全項目是正。
      - **P0-4 派生インシデント（同日封鎖）**: テスト隔離の穴（旧配置フォールバックが env 上書きを無視）により pytest が**本番台帳へ端末行を作成**していた → 「データルート上書き時は旧配置フォールバックを無効化」で根治＋conftest ガードで機械凍結（実測: 修正後の全回帰894件で本番DBへの新規書込みゼロ）。
      - **ID 53（Serve 経由端末の台帳アイデンティティ）**: ✅ 完了 (2026-09-23) — 詳細は §13.19-20。
      - **ID 50（端末自己生成UUIDによる識別恒久化）**: ⏭ **次セッション先頭の最優先タスク**（現行の (IP, 端末名) マッチは IP 変更・Serve/LAN 併用で行が増えるため、PWA 生成 UUID へ移行）。
      - 関連ADR: AD-1（失効のチャネル横断不変条件）／AD-2（暗号鍵の保存境界）／AD-3（失効の実効性 = 認証経路の読み取り専用化＋非ループバック・マスターキーの台帳必須化）を codebase-memory の DECISIONS に永続化。クイックウィン: P1-N1 unsubscribe 所有者検証（+3行）・`cryptography` の requirements 追記（1行）。
  13. **【P3】sync_ai_dotfiles.py の Knowledge Vault エクスポート失敗修正**:
      - 背景: 実行時に「Knowledge Vault エクスポート失敗: no such column: insight_key」WARNING。**知識の宝庫（insight）がバックアップから欠落する DR ギャップ**（2026-09-22 発見・本日の insight ID 6〜10 等が対象）。
      - 方針: エクスポートSQLを現行 schema に整合＋「最新 insight が含まれる」回帰テスト追加。
      - 優先度: 低 ／ 手帳 TODO **ID 43** と同時起票
   14. **【P1】Host ヘッダー許可リスト導入（DNS Rebinding 対策 / ループバック無条件マスターキー配布の是正）**:
      - 背景: P0-1 の独立検証（devils-advocate / 2026-09-22）で検出。`do_GET`/`do_POST` 冒頭に Host 検証が無く、悪意サイトが `evil.com` を 127.0.0.1 へ再解決させると、ブラウザ経由でループバック扱いの `GET /api/auth/token` が叩かれ**マスターキーが配布**され得る（Chrome の Private Network Access 頼み＝Fail-Open）。
      - 方針: ① `Host` ヘッダーを許可リスト（`localhost:port` / `127.0.0.1:port` / 自機 LAN IP:port）と照合し不一致は 403 ② ループバック経路のマスターキー配布も短命・ワンタイム化を検討。
      - エビデンス: `local_sync_server.py` `_handle_auth_token`（ループバック無条件配布）/ `_check_auth`（GET ループバック素通し）。手帳 TODO **ID 47** と同時起票。
   15. **【P2】失効×復帰のレース硬化・ペアリング窓の可視化・403自動再ペアリング停止**:
      - 背景: 同検証で検出。① `_handle_auth_token` の restore と PC 設定 UI の revoke が競合すると、直前に人間が切った端末が数ミリ秒窓で復帰し得る（`revoked_at` を用いた CAS が必要）② 承認ダイアログは UA 由来名のみ表示し「失効済み端末の復帰を含む」警告が無い ③ PWA は 403（失効）でも `/api/auth/token` を自動要求するため、失効端末が無操作で承認ダイアログを再発させ得る（`pet_auth.js` の自動リトライを 401 のみに限定）。
      - 方針: `devices.revoked_at` 追加＋`restore_device` の CAS 化 / 承認ダイアログへの失効復帰警告 / `authFetch` の 403 リトライ停止。手帳 TODO **ID 48** と同時起票。
   16. **【P3】401/403/429 エラー応答ブロックの Deep Module 化（重複8コピー）**:
      - 背景: `local_sync_server.py` に「send_response＋Content-Type＋CORS＋end_headers＋JSON ボディ」の同型ブロックが8箇所以上。P0-2（承認ポリシー）改修と同一コミットで `_write_error_json(status_code, message)` へ集約するのが効率的。
      - 優先度: 低（P0-2 と併せて実施）。手帳 TODO **ID 49** と同時起票。
   17. **【P1】ペアリング承認の1クリック横取り防止 ＋ 台帳行マッチの属性依存解消**:
      - 背景: P0-1 第2ラウンド検証（devils-advocate）で検出。① 承認ダイアログの10秒デバウンスと合流キー `(client_ip, device_name)` が `register_device` の行再利用キーと同一のため、LAN 上の攻撃者が被害端末の IP を乗っ取り UA を "iPhone" 偽装すると、ボスの1クリック承認に相乗りして行再束縛＋復帰まで成立する ② `ORDER BY last_seen DESC LIMIT 1` の行再利用は、同一モデル2台が同じ DHCP IP を使うだけで**無関係な現役端末のトークンを無言で無効化**し得る（資格情報 DoS）。
      - 方針: 承認・再利用のキーを `(ip, name)` から**ペアリング固有 nonce / 端末生成 UUID**（PWA 側 `crypto.randomUUID()`）へ変更。承認ダイアログに初回 UA・行 ID・created_at を表示。手帳 TODO **ID 50** と同時起票。
      - ✅ **完了 (2026-09-23)**: `devices.device_uuid` 列（冪等 ALTER＋**UNIQUE 索引**）を追加し、`register_device` は **UUID 一致を最優先**で行再利用。フォールバック（IP+名前 → 名前+UA）は **`device_uuid IS NULL` の行に限定**し、UUID 記名済み行はヘッダを外したクライアントからも**乗っ取れない**（査読 P1-1 是正）。UUID 提示クライアントにもフォールバックを開放することで UUID 導入前の旧行（NULL）を採用して UUID を束縛＝**移行ゾンビ行を作らない**（P2-1）。PWA は `crypto.randomUUID()`＋非セキュアコンテキスト用 `getRandomValues` フォールバックで UUIDv4 を生成し localStorage へ永続化、`X-Device-UUID` ヘッダで申告（**認証の根拠にはしない**＝AD-3 不変）。端末一覧は「端末ID: xxxxxxxx…」を表示。バージョン **1.1.8**（`WEB_PET_CACHE_NAME` 含む）。テスト: `tests/test_id50_device_uuid_identity.py`（13件）。
      - ⚠️ **残余（査読 P1-2）**: localStorage は**オリジン分離**のため、同一スマホが Serve（https）と LAN（http）の両方で開くと UUID が別々になり行が2本になる（各オリジン内では安定）。**推奨運用: Tailscale（Serve）URL に一本化**（Web Push も Serve 前提）。クロスオリジン集約は手帳 TODO **ID 58** として起票済み。
   18. **【P2】端末管理UIの暗黙物理削除・DTO deny-list の浅さ・Webhook 認証の Fail-Open**:
      - 背景: 同検証で検出。① `ui/device_manager_panel.py` の一覧表示が毎回 `cleanup_loopback_devices()`（物理 DELETE）を実行し、失敗は `except Exception: pass`（ログゼロ＝AGENTS §3 違反）。行の IP が loopback に更新された正規端末が一覧から消える ② `sync_dtos` の deny-list はトップレベル `sync_token` のみ（別名・ネストは素通り）③ `_check_webhook_auth` step2 が**マスター鍵を台帳照合なしで全 IP 許可**（A案と矛盾・順序依存の二重ゲート）。
      - 方針: 削除の明示操作化＋`logger.error` 必須化 / deny-list の再帰走査（または allow-list 反転）/ Webhook 認証のマスター鍵分岐を台帳照合必須へ統一。手帳 TODO **ID 51** と同時起票。
   19. **【P1】respond_checked の自己承認判定をチャネル資格ベースへ**:
      - 背景: P0-3 第3ラウンド検証で検出。`requester_ip` と `responder_ip` の**文字列一致**で自己承認を拒否しているため、①Tailscale Serve 経由の正規スマホは常に 127.0.0.1 同士となり**承認不能**（コアバリューのデグレ）②IPv4/IPv6 ループバックの混在（`127.0.0.1` vs `::1`）で自己承認が成立し得る。
      - 方針: 「要求元が loopback の ask を承認できるのは PC GUI または台帳の個別トークンを持つ外部端末のみ」という**資格ベース判定**へ。Serve の `X-Forwarded-For` を実IP表示・監査に使用。手帳 TODO **ID 52** と同時起票。
   20. **【P2】Serve 経由ペアリング端末の台帳アイデンティティ修正**:
      - 背景: 同検証で検出。`reuse_identity` は `127.*` を対象外とするため、Serve 経由スマホの再ペアリングは毎回新規行を作り失効行がゾンビ残存（復帰監査 `source=pairing` が記録されない）。さらに `cleanup_loopback_devices` が実スマホ行を「ゴミ」と誤認して削除しトークンを即死させる。
      - 方針: 台帳へ実IP（Serve の XFF）または `via` 列を記録し、cleanup は「実TCPピアが loopback かつ中継なし」に限定。手帳 TODO **ID 53** と同時起票。
      - ✅ **完了 (2026-09-23)**: `_resolve_ledger_client_ip()` = 中継経由（TCPピア loopback ＋ loopback信頼3条件不成立）では XFF の**単一・非loopback・有効IPのみ**を実IPとして採用（XFF 欠落/複数/不正/loopback値は `None`＝毒値 127.0.0.1 を返さない）。適用: `_handle_auth_token`（承認ダイアログは「中継経由・推定」ラベル付き・台帳登録・監査actor・UAログ）と `_check_auth`（台帳照会・last_seen・**レートリミットのキーも実IPへ統一**）。
      - `reuse_identity` を拡張: IP不明時は (端末名, UA) で同一端末を再利用（人間承認済みペアリング経路限定は不変）。
      - `cleanup_loopback_devices` は**失効済み行のみ削除**へ制限（有効資格情報の即死を構造的に防止）＋端末管理パネルの一覧から自動呼び出しを撤去。中継以前の残存ループバック行は「⚠️ 中継以前の残存行」として**表示し失効・削除可能**に（不可視資格情報の根絶）。
      - 検証: `tests/test_id53_serve_client_identity.py`（10件）／既存契約更新（`test_device_individual_tokens` の cleanup・`test_device_registry` の非ブラウザUA表示）。実機障害（2026-09-23 スマホ再承認地獄）の真因を封鎖。
   21. **【P2】外部 Webhook 認証の死にコード解消 ＋ 定数時間比較**:
      - 背景: 同検証で検出。`do_POST` が全 POST に `_check_auth` を強制するため `_check_webhook_auth` の Secret 経路（n8n 等）は事前 401 で**到達不能**。共有シークレット比較も `==`（タイミング漏洩）。
      - 方針: webhook 経路のみ `_check_auth` 前倒しを免除し `_check_webhook_auth` へ一本化＋`hmac.compare_digest` 化（不要なら撤去＝引き算）。手帳 TODO **ID 54** と同時起票。
   22. **【P1】管理系APIの loopback 専用リスナ分離（プロキシ善意への依存排除）**:
      - 背景: 同検証で検出。loopback 信頼の3条件は「Host を正直に転送し XFF を付与するプロキシ」という**外部の善意**に依存しており、nginx 既定（Host を 127.0.0.1:8765 に書き換え・XFF なし）や SSH `-L` 等の生中継を前置されると全条件が成立して承認RCEチェーンが復活する。
      - 方針: 管理系（`/api/agent/*`, `/api/devices/*`）を **loopback 専用セカンダリリスナ**（例 `127.0.0.1:8766`・Serve の proxy 対象外）へ隔離し、Serve 配信面は PWA 同期 API＋端末トークンのみに絞る。手帳 TODO **ID 55** と同時起票。
   23. **【P2】.env（APIキー）のデータ境界移行**:
      - 背景: P0-4 で DB・マスタートークン・バックアップは `%LOCALAPPDATA%` へ移行済みだが、`.env`（クラウドAPIキー）は依然リポジトリ（OneDrive 配下）に残る（ADR-2 の片肺）。
      - 方針: `app_paths.ensure_env_file` / `llm_factory.ENV_PATH` / `i18n` / `weather_tools` / `ics_tools` / `sync_config` / `ui/settings_window` 等の `.env` 参照をデータルートへ切替＋旧 `.env` の退避移行＋全参照の一斉更新テスト。手帳 TODO **ID 56** と同時起票。
   24. **【P3】test_calendar_ui_smoke の skip 判定決定論化**:
      - 背景: `setUp` 内の `tk.Tk()` 実行時判定により skip 数が 0〜2 で変動（agent-tester 実測: 3↔4 揺れ）。回帰の決定論性を損なう。
      - 方針: import 時判定（`pytest.importorskip` 相当）へ変更し skip 数を固定化。手帳 TODO **ID 57** と同時起票。
   25. **【P2】Google Colab MCP ＋ Kaggle API 連携環境の初期セットアップ**:
      - 背景: 重い処理・外部負荷試験・大量データ処理・夜間放置バッチ用に、Google Colab MCP (`colab-mcp`) の Antigravity 登録と、Kaggle API トークン (`kaggle.json`) の配置を行う。`GEMINI.md` の CLOUD 規約に準拠。
      - 方針: `kaggle.json` の `~/.kaggle/` 配備、`colab-mcp` の Antigravity 設定追加。手帳 TODO **ID 59** と同時同期。
   26. **【P3】E2E 承認フローのフレーク調査（Ubuntu 3.12 単発失敗）**:
      - 背景: 2026-09-24 の CI（`e3101d3`）で `tests/test_agent_approval_flow_e2e.py::TestAgentApprovalFlowE2E::test_auto_allow_flow` が **Ubuntu 3.12 のみ失敗**（他4プラットフォーム緑）＝タイミング/ポート依存のフレーク疑い。
      - 方針: ①失敗ログの完全取得 ②待機条件の決定論化（固定 sleep → イベント/ポーリング）③同ジョブ3回連続再実行で安定確認。手帳 TODO **ID 60** と同時起票。
   25. **【P2】Google Colab MCP ＆ Kaggle API 連携環境の初期セットアップ**:
       - 背景: 重い処理・外部負荷試験・大量データ分析・夜間放置バッチ用に、Google Colab MCP (`colab-mcp`) の Antigravity 登録と、Kaggle API トークン (`kaggle.json`) の配置を行う。`GEMINI.md` の CLOUD 規約に準拠。
       - 方針: `kaggle.json` の `~/.kaggle/` 配備、`colab-mcp` の Antigravity 設定追加。手帳 TODO **ID 59** と同時同期。