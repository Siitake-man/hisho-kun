# 学習メモ_20260310

## サマリ
本日はPhase 3（UI統合）を完了した。AIが登録した予定をカレンダー風ウィンドウで表示する機能と、付箋をデスクトップ上に常駐表示する機能を実装。
さらに付箋のUX改善（直接編集・DB連動削除）を通じて、「ポーリング vs イベント駆動」という設計パターンの違いを実践的に学んだ。
最後に、TickTickの全機能を網羅した機能ロードマップを作成し、今後の開発方針を整理した。

**今日のキーワード**: `ctk.CTkToplevel` / `tk.Text` / イベント駆動設計 / CRUD操作の完成

---

## 詳細学習内容

### 1. CustomTkinterのトップレベルウィンドウ（CTkToplevel）

#### What（何をするものか）
メインウィンドウ（`ctk.CTk`）とは別に、独立した「サブウィンドウ」を生成するためのクラス。今回はカレンダーウィンドウと付箋ウィンドウに使用した。

#### Why（なぜ必要か）
デスクトップ常駐アプリでは、「秘書くん本体」「カレンダー」「付箋×N枚」といった複数のウィンドウが同時に存在する必要がある。
それぞれを独立したウィンドウとして管理することで、画面のどこにでも自由に配置でき、不要な時は個別に閉じられる。

#### Where/When（どこで使うか）
- `CalendarWindow(ctk.CTkToplevel)` → カレンダーUI
- `StickyNoteWindow(tk.Toplevel)` → 付箋UI（タイトルバーを消すため素のtkinterのToplevelを使用）

#### 身近な例えでの理解
メインウィンドウが「事務所の本棚」だとすると、CTkToplevelは「机の上に広げる資料ファイル」。
必要な時に引っ張り出して机に広げ、用が済んだら片づける（閉じる）。

---

### 2. ポーリング vs イベント駆動 ← 今日最大の学び

#### What（何が起きたか）
最初の実装では、`main.py` のメインループ内で**3秒おきにDBから付箋データを取得する「ポーリング」**を実装した。
しかしユーザー（自分）テストの結果、以下の問題が発覚：
- ✖ボタンで付箋を閉じても**3秒後にゾンビのように復活する**
- ログが「2件の付箋を取得しました」で**埋め尽くされる**
- 編集中のテキストが上書きされるリスクがある

#### Why（なぜ問題だったか）
**ポーリング（定期巡回）** は「変更があったかもしれないから定期的に確認しに行く」方式。
これは単純だが、以下のデメリットがある：
- **無駄な処理**: 変更がなくても毎回DBに問い合わせる → CPU・メモリの浪費
- **同期の衝突**: ユーザーが編集中でもDBの古いデータで上書きしてしまう可能性
- **ゾンビ問題**: 画面上で消しても、DB側にデータが残っていると次の巡回で復活する

#### 対策（何を変えたか）
**イベント駆動（Event-Driven）** に切り替えた。具体的には：
1. **アプリ起動時**: DBから付箋を1回だけ読み込む
2. **AI推論完了時**: AIがDB操作した可能性があるタイミングで1回だけ読み込む
3. **ユーザー操作時**: 編集完了（フォーカスアウト）や削除ボタン押下時に即座にDBを更新

→ 「何か起きた時だけ動く」ため、無駄な処理がゼロになる。

#### 身近な例えでの理解
- **ポーリング** = 3秒おきに冷蔵庫を開けて「新しい食材入った？」と確認し続ける人
- **イベント駆動** = 「買い物から帰ってきた時だけ」冷蔵庫を確認する人

どちらが効率的かは一目瞭然。

> **PM視点での気づき**: ポーリングは「実装が簡単」だが「運用コストが高い」。
> イベント駆動は「実装がやや複雑」だが「運用コストが低い」。
> これはインフラ設計でも同じ原理（プルモデル vs プッシュモデル）が適用される。

---

### 3. CRUD操作の完成（付箋のUpdate/Delete追加）

#### What（何をするものか）
CRUD = Create（作成）/ Read（読み取り）/ Update（更新）/ Delete（削除）の4操作。
データの管理に必要な最低限の操作セット。

もともと付箋はCreate（作成）とRead（一覧取得）しか実装されていなかった。
今回、`database.py` に `update_sticky_note()` と `delete_sticky_note()` を追加し、4操作すべてが揃った。

#### Why（なぜ必要か）
CRUDが揃って初めて「データの完全なライフサイクル管理」ができる。
- Createだけ → ゴミが溜まる一方
- Create + Read →見るだけで変更できない
- Create + Read + Delete → 修正ができない
- **Create + Read + Update + Delete** → 完全な運用が可能

#### 身近な例えでの理解
CRUDは「手帳の使い方」に似ている：
- **Create** = 手帳に新しい予定を書く
- **Read** = 手帳を開いて予定を確認する
- **Update** = 書いた予定の時間を書き換える
- **Delete** = 不要になった予定を消しゴムで消す

4つ揃って初めて「使える手帳」になる。

---

### 4. Tkinterの `tk.Label` vs `tk.Text`（表示と編集の違い）

#### What
- `tk.Label` = テキストを**表示するだけ**のウィジェット。ユーザーは文字を編集できない。
- `tk.Text` = テキストを**表示＆編集できる**ウィジェット。メモ帳のような自由入力が可能。

#### Why（なぜ変更が必要だったか）
最初の付箋は `tk.Label` で実装されていたため、AIが書いた内容を「眺める」ことしかできなかった。
ユーザーが直接内容を書き換えたいという要件に対応するため、`tk.Text` に変更した。

#### Where/When
ユーザーの入力を受け付ける必要がある場面では `tk.Text`（または `tk.Entry`）を使う。
`tk.Entry` は1行入力向け、`tk.Text` は複数行入力向け。

---

### 5. FocusOut イベントとデータの自動保存

#### What
`<FocusOut>` はTkinterのイベントで、「あるウィジェットからフォーカス（入力カーソル）が外れた時」に発火する。
付箋の `tk.Text` にこれをバインドし、ユーザーが付箋の外をクリックした瞬間にDBへ自動保存する仕組みを実装した。

#### Why
「保存ボタン」を押させるUIは煩わしい。
フォーカスが外れた＝編集が一段落した、というユーザーの暗黙の行動をトリガーにすることで、UXが格段に向上する。

#### 身近な例えでの理解
スマホのメモアプリ（iPhoneの「メモ」等）を思い出すと分かりやすい。
あれも「保存ボタン」はなく、アプリを閉じた（フォーカスが外れた）瞬間に自動保存される。

---

## 本日の成果まとめ

| Phase | ステータス | 内容 |
|-------|-----------|------|
| Phase 1: GUI骨格 | ✅ 完了 | CustomTkinter + 非同期ループ |
| Phase 2: DBツール統合 | ✅ 完了 | LangGraph + SQLite + ツール |
| Phase 3: UI統合 | ✅ 完了 | カレンダーUI + 付箋UI + 同期最適化 |
| 機能ロードマップ | ✅ 作成 | TickTick全機能を網羅した約70項目 |

## 次回の作業候補
- タスク管理機能（TODO）のDBスキーマ追加とAIツール実装
- リマインダー（通知）機能の基盤
- UIのレトロデザイン強化（フォルダ風ウィンドウ、罫線ノート背景）
- オートハイド＆ホバー表示の仕組み
