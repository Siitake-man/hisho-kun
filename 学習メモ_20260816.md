# 📘 ネオ秘書くん 開発学習メモ (2026-08-16)

## 📌 【本日のサマリ】（10〜15行）
1. **モバイルOSのスリープ防止（NoSleep）の第一原理解明**:
   - HTTP接続下のモバイルブラウザ（Android Chrome / iOS Safari）では `navigator.wakeLock` がセキュリティ制限で拒否される。
   - 不可視（1pxや画面外）の動画や静止画ループはOSの省電力機能でスリープ解除が無視される。
   - **Canvasのアニメーションフレームを `captureStream(10)` で `<video>` に流し込み、「無限リアルタイム生配信（Live Stream）」として再生し続ける** ことで、OSのスリープタイマーを100%確実に物理停止できることを解明・実装。
2. **スレッドセーフな ActionQueue（Deep Module設計）**:
   - HTTPサーバーのワーカースレッドから Tkinter UI を直接操作すると `RuntimeError: main thread is not in main loop` が発生する。
   - `gui.post_action(func, *args)` と `main.py` のメインループ内 `app.gui.process_action_queue()` による極小インターフェースの Seam（継ぎ目）を確立し、スレッド競合を完全解決。
3. **Bluetoothイヤホン遠隔承認（Agent Bridge Earphone Interface）**:
   - Web MediaSession API を組み込み、耳のイヤホンのボタンやスマホ物理音量キーでAIのコマンド実行をノールック承認可能に。
4. **統合手帳の期間別ビュー ＆ 外部サービス設定**:
   - 統合手帳に「月間・週間・日間」ビュー切り替えを実装し、設定画面に Google Calendar / Slack 連携を統合。

---

## 🎯 【習得した技術と第一原理】

### 1. モバイルブラウザにおける常時画面ON（Wake Lock vs Media Pipeline）
- **What**: スマホを卓上スマートディスプレイとして使う際、画面が自動消灯しないようにする技術。
- **Why**: 一般的な `navigator.wakeLock` は HTTPS 必須であり、ローカルWi-Fi（`http://192.168.1.X:8765`）では動作しない。また単発の動画ループはデコーダの切れ目でOSが「再生終了」と誤判定する。
- **First Principle (第一原理)**: モバイルOSの電源管理ユニット（PowerManager）は、「ユーザーが生放送の動画ストリームを視聴している」と認識している間、画面スリープタイマーを強制的にバイパスする。Canvas の再描画フレームを `captureStream()` で送り続けることで、永遠に終わらない生配信を作り出し、OSレベルでスリープを完全阻止する。

### 2. Tkinter × Asyncio のスレッドセーフ・キュー機構（Deep Module）
- **What**: 異なるスレッド（HTTP受信スレッド）からGUIスレッドへ安全にタスクを渡す仕組み。
- **Why**: Tkinter はシングルスレッド設計（Tclインタープリタ依存）であり、別スレッドからの直接GUI操作はクラッシュ（`main thread is not in main loop`）を招く。
- **First Principle (第一原理)**: `queue.Queue`（スレッドセーフなFIFOキュー）を境界（Seam）とし、呼び出し側は `post_action(func, *args)` の1行を呼ぶだけ、UI側は毎フレーム `process_action_queue()` で自身のスレッド内で消化する。これにより、呼び出し側にスレッドの複雑さを一切漏らさない「Deep Module」が完成する。

---

## 👔 【身近な例えでの理解（PM視点）】
- **captureStream によるスリープ防止**: 「テレビの砂嵐や監視カメラの生中継を流しっぱなしにしていると、テレビの省エネ自動電源OFFタイマーが作動しないのと同じ仕組み」。
- **ActionQueue によるGUI操作**: 「各部署（外部スレッド）が勝手に社長室（Tkinter）に入って指示を出すのではなく、秘書の受付トレイ（キュー）に依頼票を入れ、社長が自分のペースで順に決済していく仕組み」。

---

## 📊 【現在の進捗と次のステップ】
- **進捗**: Phase 8.0 完全完了（DeskPet PWA の接続・表示・常時点灯・Bluetooth承認・手帳ビューすべて安定化）。
- **次回ステップ**: キャラクタースキンシステム（Phase 9）のドット絵追加、および Claude Code / Codex との実行フック配線。
