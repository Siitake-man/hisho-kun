# 🧠 学習メモ: 2026-08-15 (Phase 4: MentisDB型長期記憶・ドット絵ペット・MCP連携・統合手帳)

## 📌 今日のサマリ（10行要約）
1. **Multi-LLM 動的探索 ＆ 永続化**: OpenCode GO (DeepSeek-V4-Flash/V3/R1), LM Studio, Gemini を動的探索し、選択状態を `.env` と `discovered_models.json` にキャッシュ永続化。
2. **MentisDB型 長期知見エンジン**: SQLite に `user_insights` テーブルを設け、ボスの制約・好みを会話から自律検知して永久保存。推論時に自動プロンプト注入。
3. **レトロドット絵マスコットUI**: 仮の茶色い丸を本物のドット絵スプライトへ進化させ、待機（瞬き）・思考中（アンテナ点滅）・成功（笑顔）のアニメーションを実装。
4. **TODOタスク管理基盤 (TickTick仕様)**: `tasks` テーブルとAIツール（`create_task_tool`, `list_tasks_tool`, `complete_task_tool`）を統合。
5. **外部MCP連携 (Model Context Protocol)**: `mcp_manager.py` を実装し、設定画面から外部MCPサーバー（Filesystem/Fetch/GitHub）を動的バインド可能に。
6. **統合手帳ウィンドウ (Notebook)**: 予定・TODOタスク・ボスのトリセツ（記憶）を3タブで一元管理できるレトロ手帳UIへ全面刷新。
7. **吹き出し長文オーバーフロー根本改修**: 吹き出しをスクロール対応 `CTkTextbox`（高さ140px固定）に刷新し、どんな長文でも入力欄とペットが一切潰れない強固なレイアウトを確立。

---

## 🌟 【ドラマ・感情・判断理由】本日の開発ドキュメンタリー

### 1. 「ボスを理解し続ける相棒」への覚醒 (#ドラマ #第一原理)
- **課題**: 通常のLLMアプリは、アプリを再起動すると「前に話したこと（ボスの家族の事情や生活リズム）」をすべて忘れてしまう。
- **判断理由**: 単なるカレンダー操作ボットではなく、[[MentisDB]] の設計哲学を応用し、SQLite内に `user_insights` テーブルを構築。推論開始時に「ボスの重要知見トップ6」をシステムプロンプトへ自動注入するアーキテクチャを採用した。
- **感動の瞬間**: ユーザーが「中村天風って知ってますか？」と問いかけた際、AIが自律的に `remember_user_insight_tool` を呼び出し、「中村天風の心身統一道に感銘を受け実践している」というボスの人生哲学を永久記憶した。

### 2. 吹き出し崩壊の危機と、スクロールテキストボックスへの昇華 (#ドラマ #判断理由)
- **課題**: ペットがWindows修復手順などの長文を熱心に説明した際、吹き出しが画面外まで伸びて入力欄とペットが消失。
- **判断理由**: `CTkLabel` の動的伸長に頼るのをやめ、高さ140px固定の `CTkTextbox`（内部自動スクロールバー付き）に構造刷新。
- **成果**: どんな超長文（1000文字でも）が返ってきても、入力欄とドット絵ペットが絶対に1ミリも動かない鉄壁のUIが完成。

---

## 📚 【習得した技術と原理原則】

### 1. 動的プロンプト注入（Dynamic Context Injection）
- **What**: ユーザーごとの生活リズムや制約をDBから読み出し、LLMのシステムプロンプトに動的に埋め込む技術。
- **Why**: ファインチューニング（モデルの再学習）を行わずに、推論時のコンテキストだけで「ボスの専属秘書」の振る舞いを100%実現するため。
- **身近な例え**: 優秀な執事が手元に開いている **「ボスの申し送りノート」**。

### 2. Model Context Protocol (MCP) プラグイン連携
- **What**: Anthropicが提唱する標準規格で、外部ツール（GitHub, Notion, ファイル操作）をJSON-RPCで接続する仕組み。
- **Why**: 秘書くん本体のコードを書き換えることなく、設定ファイルを編集するだけでAIの能力を無限拡張するため。
- **身近な例え**: スマホにApp Storeから新しいアプリをインストールするように、**AIに新しい道具をプラグイン追加する仕組み**。

---

## 🗺 【現在の進捗と次のステップ】

- **進捗率**: 予定・付箋・長期記憶・ドット絵ペット・TODO手帳・MCP設定がすべて稼働！
- **次期タスク (5分ファーストアクション)**:
  1. `capture_screen` ツールを `db_tools.py` に追加し、画面キャプチャをAIに渡す MiniCPM型 画面見守り機能のプロトタイプを作成する。
