# ネオ秘書くん 開発学習メモ (2026-08-15)

---

## 📌 今日の学びサマリ（10〜15行）
- **物理的AIコックピット（Physical AI Cockpit）の実現**: 古いスマートフォンを机上の意思決定デバイスとして再利用し、PC画面を1ミリも占有せずにコーディングエージェント（Codex / Claude Code / Antigravity）のコマンド実行承認を親指ワンタップで完結させるアーキテクチャを確立した。
- **シングルスレッドHTTPServerによるブロッキング問題の解決**: `HTTPServer` が承認待ち処理（`req.wait()`）でスレッドを180秒間占有し、スマホからの状態同期通信（`GET /api/status`）が全停止していた真因を特定。`ThreadingHTTPServer` への換装により、並行非同期通信を完全実現した。
- **モバイルブラウザ（Android/Brave）のスリープ防止ハック**: 非HTTPS環境で `navigator.wakeLock` がブロックされる制約に対し、ペット描画Canvasのリアルタイムストリーム（`captureStream()`）を本物の `<video>` タグとして画面中央で常時再生させることで、OSの Display Sleep を物理的に阻止する技術を実装した。
- **Tkinterのウィンドウ非表示仕様**: 背景透過・枠なしウィンドウ（`overrideredirect(True)`）では `root.iconify()` がOS側で無効化されるため、`root.withdraw()` / `root.deiconify()` を使用してPC画面占有ゼロ化を達成した。

---

## 1. アーキテクチャ概要 (Why / What / Where)

### 【What: 今回構築したシステム】
1. **Agent Bridge Hub (`local_sync_server.py`)**:
   - `ThreadingHTTPServer` によるマルチスレッドHTTPサーバー（ポート 8765）。
   - 外部CLIツール（Codex / Claude Code 等）からの承認要請（`POST /api/agent/ask`）を受け付け、スマホ側へ中継。
   - スマホからの意思決定（`POST /api/agent/respond`）を待機中のエージェントへ同期返却。
2. **Desk Pet PWA & Cockpit (`web_pet/index.html`, `web_pet/pet.js`)**:
   - 横置き全画面2ペインUI（左: ペット・時計 / 右: 承認カード・TODO/予定）。
   - ペット自身のアニメーションを `<video>` ストリーム化し、画面消灯を物理的に防止。
   - PC側と100%完全同一のスプライト画像（PNG）レンダリング。
3. **Agent Bridge CLI クライアント (`agent_bridge_client.py`)**:
   - Codex や Claude Code、シェルスクリプトから 1 行でスマホへ承認要請を送信し、判定結果（`0`: 承認、`1`: 拒否、`2`: 説明要求）を即座に受け取るCLIツール。

---

## 2. 直面した技術的課題と根本原因（Troubleshooting Log）

### 課題1: Codex承認待機中にスマホ側へ通知が一切届かない
- **【事象】**: `agent_bridge_client.py` を実行すると待機中になるが、スマホ側には何も表示されずタイムアウト（`rejected`）する。
- **【真因】**:
  - Python標準の `HTTPServer` は **シングルスレッド** で動作する。
  - `POST /api/agent/ask` 内で `req.wait(timeout=180)` を実行した瞬間、サーバー全体の唯一の処理スレッドが停止（sleep）。
  - スマホ側から定期的に送られる `GET /api/status` や `GET /api/agent/pending` がキューに詰まり、サーバーが一切応答できなくなっていた。
- **【対策】**:
  - `from http.server import ThreadingHTTPServer` を使用し、各HTTPリクエストを独立したスレッドで処理するように改修。

### 課題2: Android / Brave ブラウザで画面が勝手に消灯（OFF）する
- **【事象】**: スマホを机の上に置いていても、1〜2分で画面が消えてしまう。
- **【真因】**:
  - `navigator.wakeLock` は **HTTPS必須** のため、ローカルIP（`http://...`）ではブラウザによって拒否される。
  - 1pxの隠しビデオや無音オーディオのみでは、Android OSの省電力マネージャーが「画面上に実質的に見えている動的コンテンツがない」と判定して消灯する。
- **【対策】**:
  - 画面中央のペットアニメーション（Canvas）から `captureStream(10)` でメディアストリームを生成し、ペット表示枠の `<video autoplay loop muted playsinline>` にバインド。
  - 画面中央で常に本物の動画が再生されている状態を作ることで、OSの Display Sleep を完全に阻止。

### 課題3: PC側ペットの自動最小化が効かない
- **【事象】**: スマホ接続時にPCペットを最小化しようとしても、画面上に残ったままになる。
- **【真因】**:
  - 背景透過のために `root.overrideredirect(True)`（タイトルバーやOS枠なし）を使用しているウィンドウでは、Windows OSの仕様上 `root.iconify()`（タスクバー最小化）が無効化される。
- **【対策】**:
  - `root.withdraw()`（完全非表示）と `root.deiconify()`（再表示）に切り替えることで、画面占有ゼロ化を達成。

---

## 3. 次のステップ（Next Milestones）

1. **実用コーディングエージェントとの連携検証**:
   - Claude Code や Codex、Antigravity のフック/スクリプトとして `agent_bridge_client.py` を組み込み、実開発フローで運用。
2. **キャラクタースキンシステムの拡充 (Phase 8)**:
   - キノコ君、3本毛棒人間、もちもちアザラシ、まるまるウォンバットのドット絵追加とスキン切り替え。
3. **オフラインBluetooth PANの社内ワンタッチ配布パッケージング**:
   - Wi-Fiがない環境や社内セキュリティ環境でも、Bluetoothペアリングだけで誰でも5秒で接続できるインストーラーの整備。
